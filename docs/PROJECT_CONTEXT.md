# Project Context

> **This is the single source of truth for the Pant Public School Digital Platform.** Every AI assistant, engineer, or contributor must read this file first, before reading any code or any other document. If information here conflicts with another document, this file wins — fix the other document, don't override this one. Other documents in `/docs` exist to hold detail that would make this file too long; they are supporting references, not competing sources of truth.
>
> No chat history should ever be required to understand this project. If something important isn't captured here or in the linked reference docs, it doesn't count as known.

---

## 1. Project Vision

Pant Public School (Vidyadhar Nagar, Jaipur, Rajasthan) is building a digital platform to replace fragmented, paper-based, and WhatsApp-driven school operations with a single, elegant web application.

The product is **not** a "School ERP" in the traditional sense. Traditional school ERPs (Fedena, PowerSchool, ERPNext Education) are feature-dense, visually dated, and require training to use. This platform is designed to feel like **Stripe Dashboard, Linear, or Notion** — software that a non-technical teacher or school principal can use confidently on day one, without a manual.

**Guiding sentence:** _If a teacher needs help using this software, we have failed._

> **Platform framing (added 2026-07-17):** The longer-term product vision has evolved from "a platform for Pant Public School" to "a reusable School Operating System (SOS), with Pant Public School as its first real implementation." This does not change anything below — the current phase, architecture, decisions, and risks in this file remain accurate descriptions of what's actually built today, and everything here is still built for Pant Public School specifically. It changes the _direction_ things generalize in later. Full vision: [PRODUCT_VISION.md](./PRODUCT_VISION.md); what this means for the architecture: [PRODUCT_ARCHITECTURE.md](./PRODUCT_ARCHITECTURE.md); decision record: [DECISIONS.md § D-019](./DECISIONS.md#d-019--product-pivot-school-operating-system-sos-pant-public-school-as-first-implementation).

Product rationale (objectives, success criteria) lives in [PRODUCT_REQUIREMENTS.md](./PRODUCT_REQUIREMENTS.md). Vision-protection rules live in [PROJECT_GUARDRAILS.md](./PROJECT_GUARDRAILS.md).

---

## 2. Current Phase & Sprint

**Current Phase:** Website Engine v1.0 remains **frozen** (Release Readiness Review, 2026-07-17, [CHANGELOG.md § 0.14.1](./CHANGELOG.md)). Epic B's implementation continues: **Sprint 0** ([D-027](./DECISIONS.md#d-027--sprint-0-data-foundation-migrations-000-001-implemented-prisma-7-config-model-schoolstatusseed-placeholders)) built Migration 000/001; **Sprint 1** ([D-028](./DECISIONS.md#d-028--sprint-1-identity-foundation-role-as-a-lookup-table-with-accesslevel-user-merges-authjss-adapter-shape-repositoryservice-layer-introduced)) built Migration 002; a Final Identity Architecture Review ([D-029](./DECISIONS.md#d-029--final-identity-architecture-review-validators-consolidated-into-libvalidations-roleiduseraccesslevel-design-affirmed)) affirmed that design; **Sprint 2** ([D-030](./DECISIONS.md#d-030--sprint-2-academic-foundation-schoolclass-naming-classsubject-deferred-repository-retrofit-for-schoolacademicyear)) built Migration 003 (`SchoolClass`, `Section`, `Subject`); **Sprint 3** ([D-031](./DECISIONS.md#d-031--sprint-3-student-foundation-dto-layer-introduced-admissionnumber-scope-fix-two-lifecycle-operations-instead-of-one-transaction-contention-bug-found-and-fixed)) built Migration 004 (`Student`, `Guardian`, `StudentGuardian`, `Enrollment`) and introduced the first DTO layer. **Sprint 4 — Teacher Foundation** ([D-032](./DECISIONS.md#d-032--sprint-4-teacher-foundation-repository-dependency-rule-made-explicit-registerteacher-composes-createuser-updateteacherassignment-soft-deletes-rather-than-mutates)) then built Migration 005 — `Teacher`, `TeacherQualification`, `TeacherAssignment` — applied for real to the live Neon database, completing the "005 — Enrollment & assignment" migration slot Sprint 3 half-filled. Made this project's already-informal "repositories never call repositories" rule explicit and permanent in the new `docs/engineering/ENGINEERING_PRINCIPLES.md`. `registerTeacher()` composes `createUser()` (identity domain) with `createTeacher()`/`createTeacherQualification()` (teacher domain) in one transaction, per `TRANSACTION_BOUNDARIES.md`'s "Teacher onboarding" row — a service crossing domains via multiple repositories, not a repository calling a repository. `updateTeacherAssignment()` soft-deletes and replaces rather than mutating in place, preserving historical provenance. Found and fixed a second, distinct cause of the "client already executing a query" warning (Prisma's own `create()+include`-inside-transaction query decomposition — not the Sprint 3 connection-contention bug) for Teacher's own new code; confirmed the same benign pattern pre-exists, unfixed, in Sprint 3's `enrollment.repository.ts`, flagged as future cleanup rather than fixed out-of-scope. Seeded 3 generic teachers with qualifications and assignments into Sprint 2's academic structure — no Pant-Public-School-specific facts. No API, UI, Attendance, Timetable, Leave Management, Payroll, Examination, or Notifications module was built. Self-review confirmed: Attendance, Timetable, and Examination can all be built without any further change to Teacher/TeacherAssignment (all would reference `User`/`Enrollment`/`TeacherAssignment` as they already exist, not require new Teacher fields). **Sprint 5 — Attendance Engine** ([D-033](./DECISIONS.md#d-033--sprint-5-attendance-engine-openattendancesessionsubmitattendancemarkattendance-kept-as-three-distinct-operations-attendancesession-carries-its-own-schoolid-enrollment-section-scoping-enforced-in-the-service)) then built Migration 006 — `AttendanceSession`, `AttendanceRecord` — applied for real to the live Neon database, the first genuinely high-volume table in the schema (`AttendanceRecord` uses a time-ordered UUIDv7 primary key from this first migration, not retrofitted later). Four lifecycle functions in `src/services/attendance/`: `openAttendanceSession()` (idempotent get-or-create), `markAttendance()` (single-record correction primitive), `submitAttendance()` (the real batch submission — a whole section's roster upserted in one transaction, matching `TRANSACTION_BOUNDARIES.md`'s "Daily attendance submission" row exactly), `reopenAttendance()` (a pure read — no separate open/closed status exists on `AttendanceSession` to toggle). Every write path enforces that a marked `Enrollment` belongs to the session's own section, per `BUSINESS_RULES.md` § 4 and `WORKFLOWS.md` § 2. `AttendanceSession` carries its own `schoolId` directly, matching `Enrollment`'s precedent, for the oversight-view index `DATABASE_REVIEW.md` § 9 names. `upsertAttendanceRecord()` uses Prisma's `upsert`, not a check-then-create, per `TRANSACTION_BOUNDARIES.md` § 5's own explicit recommendation. Confirmed via `--trace-warnings` that Sprint 4's `create()+include`-inside-transaction warning does not recur in this sprint's own new code. Seeded 1 `AttendanceSession` with 2 real `AttendanceRecord` rows (Sprint 3's actual seed data tops out at 2 enrolled students in any single section, not 5 — documented in D-033 rather than fabricating enrollments outside this sprint's scope). No API, UI, Timetable, Leave Management, Notifications, Reports, or Examination module was built. Self-review confirmed: Examination and Reports can both be built without any further change to Attendance. With the Foundation Phase complete, a **Delivery Phase Roadmap Reorganization** planning sprint ([D-034](./DECISIONS.md#d-034--delivery-phase-roadmap-clone-per-client-model-supersedes-eventual-saas-framing-epic-reordering)) then organized all remaining work into product Epics under the explicit, clarified delivery model this platform actually uses: one Master Repository, cloned into an independent repository per client school — never a shared multi-tenant SaaS deployment, superseding `PRODUCT_ARCHITECTURE.md § 2` step 4's and `ROADMAP_V2.md`'s prior "eventual SaaS" framing (both received a surgical cross-link, not a rewrite). Produced the new [docs/product/](./product/README.md) documentation set: a reordered Epic sequence (Client Customization Framework moved earlier, parallel with Data Migration Engine, since both are cheap and block onboarding a second client), a complete Administration lifecycle design (bootstrap admin, password/role/deactivation lifecycle, no self-registration ever), a reusable Import Engine design (Upload → Map → Validate → Preview → Commit → Audit, chunked per `TRANSACTION_BOUNDARIES.md § 4`'s rollover precedent), officially-sourced research confirming no supported API/bulk-export exists for Rajasthan's RTE portal (`rajpsp.nic.in`) — manual CSV/Excel import via the Import Engine is the correct, honest approach, not scraping — a corrected client delivery playbook (fixed a real ordering bug: the original sequence tried to create a Bootstrap Admin before the database existed), a client customization checklist, and a framework/versioning strategy (client repos pull framework updates via `git merge upstream/main`, never automatic/pushed). Documentation only — no code, schema, migration, or service touched; nothing committed as part of this planning sprint. **Sprint B1 — Authentication Foundation** ([D-035](./DECISIONS.md#d-035--sprint-b1-authentication-foundation-jwt-session-strategy-corrects-d-030-empirically-confirmed-incompatible-with-credentials-only-argon2id-in-libsecurity-auth-as-its-own-service-adminteacher-route-groups-renamed-to-real-path-segments)) then built the first real slice of Epic B: Auth.js Credentials authentication (`src/lib/auth/`), Argon2id password hashing (`src/lib/security/`), a dedicated `src/services/auth/` module (`authenticateUser()`, `resolveActiveSessionUser()`), login/logout, and two-tier route protection (`src/middleware.ts` — Edge, signature-only pre-check; `src/app/admin/layout.tsx`/`src/app/teacher/layout.tsx` — Node, the authoritative database-backed check). Corrected Sprint 2's D-030 "database sessions" decision to JWT after empirically confirming `@auth/core` hard-rejects database sessions when Credentials is the only provider (traced directly to the library's own assertion source, not assumed) — the underlying requirement (instant revocation on deactivation) is preserved via the `session` callback's live repository read on every request, verified end to end: a mid-session deactivation cuts off an already-issued, still-cryptographically-valid session on its very next request. Found and fixed a real pre-existing scaffold bug: `(admin)`/`(teacher)` were parenthesized route groups (no URL segment) colliding with `ROUTES.md`'s own already-documented `/admin/*`/`/teacher/*` paths — renamed to real path-segment folders while both still held only `.gitkeep` placeholders. Seeded a Bootstrap Administrator (`prisma/seed.ts`, obvious placeholder credentials, never a production secret) — the only account created without Admin-driven provisioning, per this platform's permanent "no self-registration" rule. No User Management, Dashboard, Create User UI, Reset Password, Forgot Password, Student login, or Parent login built. **Sprint B2 — Bootstrap Administration & User Management Foundation** ([D-036](./DECISIONS.md#d-036--sprint-b2-administration--user-management-foundation-usermustchangepassword-column-centralized-libauthorization-orchestrated-not-modified-registerteachercreateidentityuser-cookie-flashed-temporary-passwords)) then closed both of Sprint B1's own open questions and built functional (not polished) User Management: Migration 007 added `User.mustChangePassword` (purely additive); `src/lib/authorization/` centralizes every permission check (`canManageUsers`/`canManageTeachers`/`canResetPasswords`/`canDeactivateUsers` + `requireSession()`/`requirePermission()`); `src/services/administration/` orchestrates the unmodified Sprint 1/4 `createIdentityUser()`/`registerTeacher()` rather than extending either, adding a shared `applyTemporaryPassword()` for account creation and Admin-initiated reset; `src/app/admin/users/` (list/search/filter, create, details, edit, reset password) and a top-level `/change-password` (the only path that clears `mustChangePassword`) were built as plain Server Component forms — the two flows needing a one-time-display secret (create, reset) were originally built with `useActionState` Client Components, then rebuilt after real end-to-end verification found that protocol unverifiable in this environment (no headless-browser tool available, `curl` simulation failing on bound Server Actions); the replacement flashes the temporary password via a short-lived `httpOnly` cookie instead, verified working end to end. Every mutation writes an `AuditLog` entry (confirmed via direct query: `CREATE`/`UPDATE`/`SOFT_DELETE` all present, no password hash or plaintext ever stored in the log). `docs/development/DEVELOPMENT_LOGIN.md` documents the Bootstrap Administrator's login/rotation, resolving Sprint B1's Open Question #1. No Student/Guardian management, Attendance UI, Notifications, Forgot Password, or Email integration built — explicitly out of scope. Two small stabilization fixes followed directly: **Sprint B2.1** ([D-037](./DECISIONS.md#d-037--sprint-b21-authentication-stabilization-root-cause-was-stale-live-credentials-not-a-code-defect)) traced a reported "Bootstrap Administrator cannot log in" bug to stale live credentials (this project's own prior end-to-end verification had genuinely changed the password and never restored it) — no code defect, fixed by resetting the row via the existing `updateUserPassword()`/`writeAuditLog()` path. **Sprint B2.2** ([D-038](./DECISIONS.md#d-038--sprint-b22-post-login-redirect-single-resolvepostloginredirect-resolver-replaces-three-independent-role-mapping-implementations-auth-does-not-see-a-same-request-signinredirectfalse-cookie-in-this-nextjs-version)) fixed a real defect — login defaulted to the public `/` homepage instead of a role-appropriate destination — via one authoritative `resolvePostLoginRedirect()` resolver, replacing three independent role-mapping implementations. **Sprint B3 — First-Time Setup Wizard & System Readiness** ([D-039](./DECISIONS.md#d-039--sprint-b3-first-time-setup-wizard--system-readiness-frameworkconfig-as-a-write-once-snapshot-not-a-live-cache-one-checksystemreadiness-service-reused-by-three-consumers-middleware-x-pathname-header-for-a-server-component-loop-safe-redirect)) then built the production-readiness gate this platform's clone-per-client model needs: Migration 008 (`FrameworkConfig`, a write-once "what was true when setup was completed" snapshot — deliberately not a live-state cache); `checkSystemReadiness()` (`src/services/system/`) as the one reusable service computing Database/Schema/Bootstrap/Roles/School/AcademicYear/Authentication/Version/Overall fresh on every call, reused unmodified by three consumers (the Setup Wizard, Admin Home, and the new `/admin/system` Developer Information page); `/admin/setup` (System Verification, School Verification with real edit capability for the first time, Bootstrap Verification linking to the existing password-reset flow, Finalize Setup); a rebuilt Admin Home (System Ready/Framework Version/Current School/Current Academic Year/Current User + Quick Actions, replacing Sprint B1/B2's bare link-list stub). `src/middleware.ts` gained a small additive `x-pathname` header (no new database read) so the Admin layout's Server Component guard can avoid redirecting `/admin/setup` to itself in a loop. Verified end to end on a live production build: first startup shows the wizard, completing it marks the deployment ready, a second login lands directly on `/admin` with no wizard detour. Named, not fixed (Epic C's own scope, not this sprint's): the generic Nursery–8 class/section/subject seed list is likely also a dev-fixture, like the Student/Teacher/Attendance seed data `CLIENT_IMPLEMENTATION_PLAYBOOK.md` already excludes from real deployments. **Sprint B4 — Audit Log Viewer & Administration Freeze** ([D-040](./DECISIONS.md#d-040--sprint-b4-audit-log-viewer--administration-freeze-writeauditlog-now-delegates-to-a-real-repository-closing-a-sprint-0-era-exception-exact-match-redaction-denylist-replaces-a-substring-match-that-redacted-its-own-safe-audit-flags-epic-b-declared-complete)) then closed Epic B's last remaining item: `/admin/audit` (Administrator-only, Server Components, GET-based Date/Action/Entity Type/Actor/Search filters and pagination — mirrors the existing `/admin/users` filter pattern), backed by a new `src/repositories/auditLog/` and `src/services/audit/`. A pre-implementation Administration Architecture Review found one genuine, long-tolerated inconsistency — `writeAuditLog()` (Sprint 0) called Prisma directly, bypassing the repository layer that didn't exist yet for that model — fixed by routing it through the new repository's `createAuditLog()` with its own external signature unchanged, so none of its ~27 existing call sites needed edits. A real bug was found and fixed during this sprint's own verification, not shipped: the first redaction implementation over-matched on "password" as a substring and redacted the safe `passwordReset`/`mustChangePassword` boolean flags Sprint B2's audit design deliberately logs instead of a real secret — replaced with an exact-match denylist, re-verified correct against a live audit entry afterward. Bootstrap Administrator and Security reviews found no inconsistencies requiring a fix. **Epic B (Administration) is now declared complete — 8/8 per `EPIC_ROADMAP.md`'s own Core+Full breakdown.** **Sprint C1 — Branding & Configuration Framework** ([D-041](./DECISIONS.md#d-041--sprint-c1-client-configuration-framework-school-db-extended-as-the-admin-facing-source-of-truth-srcconfigschoolts-remains-the-separate-public-website-source-named-divergence-not-closed-this-sprint-branding-color-boundary-reaffirmed-as-deliberate)) then began Epic C: an Architecture Review of `src/config/` classified every value as Framework/Client/Runtime-owned (confirming `src/config/` already holds zero "Daily School Data") and surfaced one real, named-not-closed finding — `School` (DB, Admin-editable since Sprint B3) and `src/config/school.ts` (static, public-website-facing) are two separate sources for overlapping facts, not currently kept in sync; closing this for real would mean moving the public website to database-driven rendering, a genuine redesign out of this sprint's scope. Built `/admin/configuration` (Migration 009 — `School` extended additively with `tagline`/`medium`/`principalName`/`principalTitle`/`email`/`phone`/`address`/`schoolTimings`/`officeTimings`/`logoUrl`/`faviconUrl`; a new `src/services/configuration/`, deliberately separate from Epic B's frozen `src/services/system/`, calling the same `School`/`AcademicYear` repositories directly). Configuration Status (Configured/Missing/Placeholder/NeedsAttention) reuses the existing bracketed-placeholder convention and `TEXT_REGISTRY.md`'s own P0/P1/P2 priorities rather than inventing a parallel system. Branding Review: logo/favicon are now genuinely client-configurable (a URL field, no file-upload system built); the primary brand color's code-only boundary was reviewed and reaffirmed as deliberate, not a gap (`CONFIGURATION_GUIDE.md § 1`). Verified end to end on a live production build, including a real edit persisting, appearing in the Audit Log, and the public website confirmed unaffected. **Sprint C2 — Client Onboarding Certification** ([D-042](./DECISIONS.md#d-042--sprint-c2-client-onboarding-certification-readmemd-quick-start-was-genuinely-onboarding-blocking-missing-migrateseedlogin-steps-prismaseedts-gains-a-seed_dev_fixtures-flag-closing-a-real-client-deployment-gap-live-dry-run-performed-against-a-temporarily-reset-deployment-state)) then performed a Business Workflow Review — "can a new TechPulse engineer onboard a school using only this repository and its documentation" — and found `README.md` genuinely onboarding-blocking: its Quick Start never mentioned running migrations or the seed script, its Status/Roadmap described the project many sprints out of date, and it never linked `docs/development/DEVELOPMENT_LOGIN.md`. Fixed directly. A second real gap: `prisma/seed.ts` had no way to skip its generic Nursery–8 classes/subjects/guardians/students/teachers/attendance dev fixtures for a real client deployment, despite `CLIENT_IMPLEMENTATION_PLAYBOOK.md`/`GO_LIVE_CHECKLIST.md` already requiring reference-data-only seeding — closed with an additive, backward-compatible `SEED_DEV_FIXTURES` environment flag, proven live both ways. The `"seed-school"` hardcoded literal, previously flagged as a candidate coupling point, was investigated and confirmed harmless (appears nowhere outside `prisma/seed.ts`). A live dry run (not code review alone) exercised the complete flow against a temporarily-reset deployment state — bootstrap login → forced password change → Setup Wizard (since no local Postgres/Docker exists in this environment to provision a literal second database) → Finalize Setup → Configuration → Administration → an existing Teacher's independent login → all 8 public website routes — confirming the framework genuinely works end to end for a new deployment, not just for Pant Public School's own already-configured one. Produced [docs/product/ONBOARDING_CERTIFICATION_CHECKLIST.md](./product/ONBOARDING_CERTIFICATION_CHECKLIST.md), a reusable artifact for certifying the next real client's onboarding. **Sprint D1 — Import Engine Architecture & Foundation** ([D-043](./DECISIONS.md#d-043--sprint-d1-import-engine-architecture--foundation-two-new-tables-importbatchimportrow-each-independently-justified-chunked-commit-is-one-transaction-per-row-not-per-chunk-matching-transaction_boundariesmd--4s-rollover-precedent-entity-agnostic-extension-points-only--no-real-importer-built)) then began Epic D: a Business Workflow Review ("a school has given us 12 Excel files, how should this framework safely migrate them") mapped the real answer — 12 files become 12 separate `ImportBatch` runs, sequenced by the Admin respecting real dependencies (Academic Structure before Students/Teachers), each walking the same Upload → Detect → Map → Validate → Preview → Commit → Audit → History → Report lifecycle. Built Migration 010 (`ImportBatch`/`ImportRow`, two tables, each independently justified per `docs/database/`'s own "every table must justify its existence" discipline) and the full entity-agnostic orchestration service (`src/services/import/`) — zero embedded knowledge of what a Student or Teacher record looks like; a future `StudentImporter`/`TeacherImporter`/`AttendanceImporter` plugs in via two extension-point interfaces (`ImportRowValidator`, `ImportRowCommitHandler`), neither implemented this sprint. The Commit stage's chunking follows `TRANSACTION_BOUNDARIES.md § 4`'s Academic Year Rollover precedent exactly — one transaction per row, not per chunk, so one bad row's failure never rolls back its already-committed neighbors — verified live via a scratch script proving chunking, resumability, and per-row failure isolation all genuinely work, not assumed from the design. `/admin/imports` is a minimal History list, correctly empty today since no real importer exists yet to populate it. **Sprint D2 — Academic Structure Importer** ([D-044](./DECISIONS.md#d-044--sprint-d2-academic-structure-importer-first-real-importer-built-as-the-reference-architecture-two-genuine-gaps-found-in-sprint-d1s-own-foundation-and-fixed-importrowvalidator-widened-async-academicservicets-gains-transaction-passthrough-importentitytype-simplified-to-one-academic_structure-value)) then built the first real importer, deliberately as the reference architecture every future importer follows: a Business Workflow Review ("a school has sent us one Excel file containing Classes, Sections, Subjects, Academic Years") mapped normalized rows → Data Profiling → grouping (one row per source line collapses into one row per unique class with its full section list, and one per subject) → Business+Database Validation → Preview → Commit → Audit → Report → History. Building the first real importer against Sprint D1's own foundation surfaced two genuine gaps, both fixed: `ImportRowValidator.validateRow()` was synchronous, but real Database Validation (duplicate detection, academic-year resolution) needs a query — widened to return a `Promise`, additively; and `createSchoolClassWithSections()`/`createAcademicSubject()` had no way to join an already-open transaction, which traced through to a real idempotency bug on commit retry (a row that actually succeeded could be re-attempted and wrongly marked `FAILED`) — both gained an optional `tx` parameter, byte-for-byte backward compatible when omitted. `ImportEntityType`'s three unused `SCHOOL_CLASS`/`SECTION`/`SUBJECT` values (D1) were replaced with one `ACADEMIC_STRUCTURE` value, matching how `EPIC_ROADMAP.md` itself always described this import type — confirmed via grep that nothing depended on the three-way split before removing it. Verified live with real, not synthetic, entity creation: a real `SchoolClass` with 2 deduped `Section`s and a real `Subject` were created through the actual, unmodified services, confirmed by direct database query and by their own normal `AuditLog` entries firing. **Sprint D3 — Upload, Detection & Smart Mapping** ([D-045](./DECISIONS.md#d-045--sprint-d3-upload-detection--smart-mapping-two-new-dependencies-papaparseexceljs-import-profiles-formalize-d2s-ad-hoc-aliases-into-a-reusable-registry-a-live-found-next_redirect-swallowed-by-trycatch-bug-fixed-in-the-mapping-action)) then built the standard Upload/Detect/Map experience every future importer reuses: `/admin/imports/upload` (file validation, `papaparse`/`exceljs` parsing behind a format-agnostic `FileParser` abstraction, SHA-256 duplicate detection), `/admin/imports/mapping` (deterministic Import Type Detection — alias-overlap scoring, no AI — pre-filled column mapping from a Saved Mapping Template or a reusable Import Profile's own alias dictionary, an Import Health Summary with a deterministic Quality Score), `/admin/imports/preview` (reuses D1/D2's own Preview/Skip/Commit pipeline completely unchanged). Import Profiles (`src/services/import/profiles.ts`) formalize what Sprint D2 hardcoded directly into its own alias dictionary — one real profile (Academic Structure) and one metadata-only profile (Student Register, aliases only, no working importer — explicitly out of this sprint's own "Do NOT build" scope). Verified twice: a scratch script exercising the service layer directly (file validation, both CSV and XLSX parsing, Detection, Data Profiling, Saved Mapping Template reuse, corrupted-file rejection), then a real HTTP walkthrough of the actual running pages — a genuine multipart upload through to a real committed `SchoolClass`, confirmed by direct database query. That second pass caught a real bug the first pass's direct function calls couldn't have: the Mapping page's success `redirect()` was inside its own `try/catch`, so Next.js's internal redirect throw was mistaken for a real error on every successful submission — fixed by moving every success redirect outside its `try` block. **Sprint D4 — Import Engine Freeze & Architecture Certification** ([D-046](./DECISIONS.md#d-046--sprint-d4-import-engine-freeze--architecture-certification-16-question-certification-against-real-code-not-assumed-one-genuine-gap-found-and-fixed-studentteacherattendanceservicets-gain-the-same-tx-passthrough-d2-gave-academicservicets-verified-live-with-a-real-rollback-test-two-narrower-composability-constraints-named-but-deliberately-not-fixed-epic-ds-core-engine-declared-frozen)) then certified Epic D against 16 mandatory architecture questions, answered from real code, not assumption. Confirmed D-044's own prediction: the `tx`-passthrough gap it fixed for `academic.service.ts` did in fact exist, unfixed, in `student.service.ts`/`teacher.service.ts`/`attendance.service.ts` — fixed identically (`registerStudent()`, `enrollStudent()`, `registerTeacher()`, `deactivateTeacher()`), plus a deeper fix for the five functions (`assignTeacher()`, `updateTeacherAssignment()`, `openAttendanceSession()`, `markAttendance()`, `submitAttendance()`) that previously read their own just-written row back only _after_ their transaction committed — now read via the same still-open transaction instead, closing a real composability gap for a future commit handler. Verified live with a scratch script, not typecheck alone: an externally-provided transaction, forced to roll back, proved a student genuinely wasn't left half-committed; a teacher-plus-assignment composed into one still-open transaction proved the read-via-tx fix actually works, not just compiles. That same live test surfaced two narrower, real constraints — named, not fixed (real future plumbing for a Teacher/Attendance importer that doesn't exist yet, not urgent for today's only real importer, Academic Structure). Results Import (Q4) and Admission Import (Q5): confirmed via schema grep that no `Examination`/`MarksRecord`/`Admission*` tables exist yet — a data-model gap belonging to Epic E/F, not an Import Engine architecture gap. Repository/service/DTO/audit/import boundaries (Q6) all confirmed clean via direct grep. **Epic D's core engine is now formally declared frozen** — Academic Structure remains its only real importer; Student/Teacher/Attendance/Result/Admission importers are each future work _against_ this now-frozen engine, not further engine changes.

**Current Sprint Goal:** Sprint E4 (Principal Workspace — "Principal 360") complete ([D-051](./DECISIONS.md#d-051--sprint-e4-principal-workspace-principal-360-a-school-operations-dashboard-enhancing-the-existing-admin-home-in-place-rather-than-a-parallel-route--principal-and-administrator-are-both-accesslevel-admin-role-rows-d-028d-029-so-no-new-accesslevel-distinction-was-needed-one-shared-per-section-computation-feeds-both-attendance-and-teacher-overview)). Epic B (Administration) remains frozen (8/8, unchanged). Epic C (Client Onboarding Engine) is engineering complete, unchanged this sprint. Epic D (Data Migration Engine)'s core engine remains frozen, unchanged this sprint. The Student and Attendance domains remain frozen (D-047, D-049), unchanged this sprint. **This is Epic E's capstone: a School Operations Dashboard, not another CRUD module** — `/admin` (Sprint B3's own Admin Home, enhanced in place rather than duplicated into a parallel route, since Principal and Administrator already share `accessLevel: ADMIN`) now shows School Overview, Attendance Overview, Teacher Overview, Student Overview, deterministic Operational Alerts, Quick Actions, and an honest Recent Activity placeholder — all composed by one new read-composition service, `getPrincipalWorkspace()`. Zero new database tables. `canViewPrincipalWorkspace()` is a named `accessLevel === "ADMIN"` check (no new AccessLevel value — Principal/Administrator are both `Role` rows sharing one tier, D-028/D-029), so Teacher remains correctly blocked by the same existing `/admin` layout guard. One shared `computeSectionStates()` helper feeds both Attendance Overview and Teacher Overview from a single per-section computation, avoiding duplicate logic. Verified live end to end, including the critical authorization boundary: a real Administrator session rendered all four Overview sections with internally-consistent real data, a Teacher session was correctly blocked (307), and the pre-filtered "Teachers" Quick Action link returned exactly the same count as the Dashboard's own Teacher Overview figure.

The public site's design system (Phase 1A) is unchanged and still accurate: semantic design tokens (including a placeholder accent color, see [DECISIONS.md § D-010](./DECISIONS.md#d-010--placeholder-accent-color-for-phase-1a)), Inter typography, a working light/dark theme toggle, a responsive sticky header with mobile navigation, a footer, and a Hero-only homepage at `/`. Phase 1B built 15 reusable section components under `src/components/website/sections/` ([D-012](./DECISIONS.md#d-012--marketing-section-library-location-componentswebsitesections-not-srcfeatures)); Phase 1B.1 independently re-reviewed and reaffirmed that placement ([D-013](./DECISIONS.md#d-013--phase-1b1-architecture-review-reaffirmed-role-segmented-components-added-a-feature-subgrouping-threshold-rule)–[D-015](./DECISIONS.md#d-015--permanent-devplayground-route-replaces-temporary-preview-and-delete-pattern)). Phase 1C composed that library into `/about` under a `components/website/pages/` page-composite pattern ([D-016](./DECISIONS.md#d-016--page-composite-folder-pattern-componentswebsitepagespage--thin-route-re-export)); Milestone 4 built `/admissions` and extracted a shared `src/lib/seo.ts` helper ([D-017](./DECISIONS.md#d-017--shared-libseots-helper-extracted-on-the-second-page)); Milestone 5 built `/academics`; Milestone 6A introduced the centralized `src/config/` layer with zero visible change ([D-018](./DECISIONS.md#d-018--centralized-configuration-layer-srcconfig)); Milestone 6B built `/campus`, the fourth content page. The product vision then pivoted to a reusable School Operating System, Pant Public School as first implementation ([D-019](./DECISIONS.md#d-019--product-pivot-school-operating-system-sos-pant-public-school-as-first-implementation), [PRODUCT_VISION.md](./PRODUCT_VISION.md), [PRODUCT_ARCHITECTURE.md](./PRODUCT_ARCHITECTURE.md)) — a documentation-only change with zero code impact, verified by an identical production build before and after. The Content Readiness Framework ([docs/onboarding/](./onboarding/README.md)) is the first artifact built explicitly _as_ platform process, not retrofitted onto it afterward. The School Life Experience milestone then built `/school-life`, the fifth content page ([D-021](./DECISIONS.md#d-021--school-life-experience-ships-with-deliberate-component-variety-not-a-repeated-gallery-grid)), and promoted `Campus`'s page-local `Prose` helper to the shared Marketing Section Library — now 16 components ([D-020](./DECISIONS.md#d-020--prose-promoted-from-page-local-helper-to-shared-component)). With Epic A substantially complete, the School Domain Model ([docs/domain/](./domain/README.md), [D-022](./DECISIONS.md#d-022--school-domain-model-board-agnostic-enrollment-centric-rte-aware)) designed the complete ~29-entity data model Epic B will eventually implement — grounded in real Indian K-8 schooling regulation (RTE, UDISE+, state-level promotion policy variance), not CBSE-only or Rajasthan-only assumptions, and explicitly not itself an implementation: zero Prisma models, migrations, or API routes exist yet. The Contact & Visit Experience milestone then built `/contact`, the sixth content page — informational only, forwarding admission-related contact to the still-planned `/admissions/enquiry` — and promoted `Admissions`'s page-local `DataTable` helper to the shared Marketing Section Library, now 17 components ([D-023](./DECISIONS.md#d-023--datatable-promoted-from-page-local-helper-to-shared-component)). The Document Center Experience milestone then built `/documents`, the seventh content page and the first to reference any downloadable document at all — its four document categories (Admission Documents, Academic Documents, Mandatory Public Disclosures, School Policies) render from a single data array rather than hand-written per-category JSX, so a future category doesn't require restructuring the page ([D-024](./DECISIONS.md#d-024--document-center-renders-categories-from-a-data-array-not-hand-written-jsx-blocks)), populating [docs/onboarding/DOCUMENT_REGISTRY.md](./onboarding/DOCUMENT_REGISTRY.md) for the first time. The Content Readiness Framework now names 97 real content requirements across all seven built pages, all at status **Required** as of this baseline (0% published) — see [docs/onboarding/CONTENT_DASHBOARD.md](./onboarding/CONTENT_DASHBOARD.md). Every unconfirmed fact across all seven pages remains an explicit bracketed placeholder, per [CONTENT_GUIDELINES.md § 12](./CONTENT_GUIDELINES.md#12-what-this-platform-never-says). Phase 0B.1's infrastructure (tooling, empty folders, runtime pin) is unchanged and still accurate — see §5/§6 below for what's built vs. still pending.

Full phase breakdown: [ROADMAP.md](./ROADMAP.md). Live sprint task board: [TASKS.md](./TASKS.md). Feature-by-feature status dashboard: [FEATURE_STATUS.md](./FEATURE_STATUS.md).

---

## 3. Current Architecture (Summary)

A single Next.js 15 application serves three role-segmented experiences (Guest, Admin, Teacher) from one codebase. No separate backend service — business logic lives in Route Handlers and Server Actions. Server Components are the default rendering mode; Client Components are the exception, justified case-by-case.

```
┌─────────────────────────────────────────────────────┐
│                  Next.js 15 App                       │
│   Public Website   │   Admin Console   │   Teacher      │
│   (Guest)          │                   │   Dashboard    │
│         └──────────────────┴──────────────────┘         │
│                  Route Handlers (API)                    │
│                       Prisma ORM                          │
└──────────────────────────┬──────────────────────────────┘
                  PostgreSQL · Auth.js · Cloudinary
```

Full architecture detail (folder structure, routing strategy, rendering strategy, state management, security and performance principles): [ARCHITECTURE.md](./ARCHITECTURE.md).

---

## 4. User Roles

Exactly **three roles** in Version 1. No others without a recorded decision in [DECISIONS.md](./DECISIONS.md).

| Role        | Description                            | Access                                          |
| ----------- | -------------------------------------- | ----------------------------------------------- |
| **Guest**   | Unauthenticated public visitor         | Public website only                             |
| **Admin**   | Principal / Management / School Office | Full administrative control                     |
| **Teacher** | Teaching staff                         | Scoped dashboard for their own classes/students |

There is **no Parent login** and **no Student login** in Version 1 — see [DECISIONS.md § D-001](./DECISIONS.md#d-001--three-roles-only-for-version-1).

---

## 5. Completed Features

- **Public website design system** (Phase 1A): semantic design tokens, Inter typography, light/dark theme toggle, responsive header (desktop + mobile nav), footer, Hero-only homepage.
- **Marketing Section Library** (Phase 1B, architecture-reviewed in Phase 1B.1): 16 reusable, prop-driven section components (`PageHero`, `SectionHeader`, `ContentContainer`, `SectionDivider`, `ImageText`, `FeatureGrid`, `StatisticsGrid`, `Timeline`, `QuoteBlock`, `CTASection`, `FAQAccordion`, `BadgeGroup`, `Callout`, `ResponsiveImage`, `AnimatedSection`, `Prose`) under `src/components/website/sections/`, each documented and verified in a real browser via the permanent dev-only `/dev/playground` route. `Prose` was promoted from a page-local `Campus` helper once `School Life` became its second real consumer ([D-020](./DECISIONS.md#d-020--prose-promoted-from-page-local-helper-to-shared-component)).
- **About page** (Phase 1C) at `/about`: the first real public content page, composed entirely from the Marketing Section Library with zero new components. Several fields hold explicit bracketed content placeholders (founding story, Principal's message, timeline dates, one achievement claim) pending School Admin input — see [COMPONENT_INVENTORY.md § Website Pages](./COMPONENT_INVENTORY.md#website-pages).
- **Admissions Experience** (Milestone 4) at `/admissions`: the full informational page a parent reads before enquiring — overview, admission journey, class-wise eligibility, required documents, fee information, FAQ, and school timings. No form. Composed from the Marketing Section Library plus one page-local `DataTable` helper (not a library component). Several fields hold bracketed placeholders (affiliation status, ages, one document requirement, all fees/timings, all FAQ answers) pending School Admin input.
- **Academics Experience** (Milestone 5) at `/academics`: teaching philosophy, four learning stages (Nursery–Class 8), broad subject areas, methodology, co-curricular activities, assessment approach, closing admissions CTA. Composed entirely from the Marketing Section Library, zero new components. Deliberately avoids naming any curriculum board or stating board-specific examination rules; several fields hold bracketed placeholders (co-curricular offerings, assessment policy specifics) pending School Admin input.
- **Configuration layer** (Milestone 6A) at `src/config/`: school-identity, branding, navigation, contact, social, and SEO-default values centralized into six framework-free data modules, replacing literals duplicated across up to 9 files. Every existing consumer (Hero, SiteHeader, SiteFooter, MobileNav, root layout, `lib/seo.ts`, and all three pages) refactored to import from it — zero visible UI change, verified in an isolated browser. Architectural improvement only, no new page or business feature.
- **Campus Experience** (Milestone 6B) at `/campus`: safety, classrooms, library, computer learning, sports, wellbeing, and a gallery preview (four placeholder categories, no real photography), closing with a visit CTA. Composed entirely from the Marketing Section Library, zero new components; alternates narrative prose with focused card grids so the page reads as a story rather than a facilities checklist. Added to primary navigation. Several fields hold bracketed placeholders (safety measures, digital learning aids, library/lab details, sports facilities, wellbeing provisions) pending School Admin input; the gallery section explicitly awaits real campus photography.
- **School Operating System (SOS) product pivot**: the product vision reframed from "a website for Pant Public School" to "a reusable platform, Pant Public School as first implementation" — [PRODUCT_VISION.md](./PRODUCT_VISION.md), [PRODUCT_ARCHITECTURE.md](./PRODUCT_ARCHITECTURE.md), [ROADMAP_V2.md](./ROADMAP_V2.md), [CONFIGURATION_GUIDE.md](./CONFIGURATION_GUIDE.md) ([D-019](./DECISIONS.md#d-019--product-pivot-school-operating-system-sos-pant-public-school-as-first-implementation)). Documentation only — verified zero code impact (identical production build before and after).
- **School Life Experience** (School Life milestone) at `/school-life`: annual events, sports & competitions, cultural activities, celebrations, and student achievements, closing with a gallery preview (positioned deliberately as supporting evidence, not the page's centerpiece), a parent testimonial placeholder, and a visit/admissions CTA ([D-021](./DECISIONS.md#d-021--school-life-experience-ships-with-deliberate-component-variety-not-a-repeated-gallery-grid)). Composed from the Marketing Section Library with deliberate component variety (`FeatureGrid` for description-bearing content, `BadgeGroup` for simple name-lists) rather than a repeated card grid. Added to primary navigation. Every event, sport, cultural offering, celebration, and achievement is an explicit bracketed placeholder pending School Admin input — none invented.
- **Content Readiness Framework** at [docs/onboarding/](./onboarding/README.md): content requirements generated directly from the built pages' implementation — not invented — organized into registries, a calculated readiness dashboard, a staff-facing collection guide, and onboarding/go-live checklists reusable for any future school. See [docs/onboarding/CONTENT_DASHBOARD.md](./onboarding/CONTENT_DASHBOARD.md) for the current, always-up-to-date count.
- **School Domain Model** at [docs/domain/](./domain/README.md) ([D-022](./DECISIONS.md#d-022--school-domain-model-board-agnostic-enrollment-centric-rte-aware)): the complete ~29-entity domain model for Epic B (School & Academic Structure, People & Identity, Admission, Enrollment & Progression, Attendance, Examination, System/Cross-Cutting), designed for independent Indian K-8 schools generally, not CBSE-only or Rajasthan-only. Architecture only — zero Prisma models, migrations, API routes, or UI built. `Enrollment` (not a mutable current-class field) is the structural hub; promotion policy, grading scale, and board affiliation are all school/year-level configuration, never hardcoded, given real state-level regulatory variance (RTE Amendment Act 2019 detention policy) this platform must not assume away.
- **Physical Database Review** at [docs/database/](./database/README.md) ([D-025](./DECISIONS.md#d-025--physical-database-review-time-ordered-ids-for-high-volume-tables-partial-unique-indexes-partitioned-audit-log)): reviewed the domain model's illustrative schema as a physical Postgres design — indexing strategy, constraint strategy (partial unique indexes for `AcademicYear.isCurrent`/`TeacherAssignment.isClassTeacher`), enum vs. lookup-table vs. string decisions, `AuditLog` append-only enforcement and partitioning, soft-delete's unique-constraint interaction, small independent migrations (`AuditLog` first, not last), and growth projections (`AttendanceRecord`: 600K–6.6M rows over 15 years for one school). Architecture only — zero Prisma schema, SQL, or migration created. Found and flagged three schema gaps for `docs/domain/DATABASE_SCHEMA.md`'s own next edit, not fixed in place.
- **Pre-Prisma Consistency Verification** ([D-026](./DECISIONS.md#d-026--pre-prisma-consistency-verification-transaction-boundaries-added-schema-gaps-closed)): cross-checked all 22 domain/database documents for entity, naming, relationship, PK/FK/index/enum/audit/soft-delete consistency. Fixed nine real inconsistencies directly — including the three schema gaps the prior review had deferred, two fields promoted from string to enum, a missing FK relation, and a genuine self-contradiction in `ERD.md`'s own diagram. Added `docs/database/TRANSACTION_BOUNDARIES.md` (a real gap: no document previously addressed which multi-table writes, including a mutation's own `AuditLog` entry, must be atomic). No document merge was found warranted.
- **Sprint 0 — Data Foundation** ([D-027](./DECISIONS.md#d-027--sprint-0-data-foundation-migrations-000-001-implemented-prisma-7-config-model-schoolstatusseed-placeholders)): the first real Prisma implementation — `prisma/schema.prisma` (Migration 000: `AuditLog`; Migration 001: `School`, `AcademicYear`), the Prisma Client singleton (`src/lib/db.ts`, driver-adapter pattern via `@prisma/adapter-pg`), Zod environment validation (`src/lib/env.ts`), reusable database utilities (`src/lib/db-utils.ts`: `checkDatabaseHealth()`, `writeAuditLog()`), and a seed script (`prisma/seed.ts`). Discovered and worked around a real Prisma 7 breaking change (schema-level `url`/`directUrl` removed; connection config now split between `prisma.config.ts` and a runtime driver adapter) and a real `.env.local`-loading bug in `prisma.config.ts`. No later migration, API route, or UI built. Not applied to a live database — none is available in this environment; schema-validated and SQL-generated only.
- **Sprint 1 — Identity Foundation** ([D-028](./DECISIONS.md#d-028--sprint-1-identity-foundation-role-as-a-lookup-table-with-accesslevel-user-merges-authjss-adapter-shape-repositoryservice-layer-introduced)): Migration 002 — `Role` (lookup table with a separate `accessLevel` tier, `ADMIN | TEACHER`), `User` (merges Auth.js's `@auth/prisma-adapter` contract with this app's `schoolId`/`roleId`/`passwordHash`/`deactivatedAt` fields), `Account`, `Session`, `VerificationToken` — applied for real to a now-live Neon PostgreSQL database (`prisma migrate deploy`, not just schema-validated) and verified with live queries. Introduced the project's first repository/service layer — `src/repositories/{user,role}/` (no direct Prisma usage outside this folder, verified by grep), `src/services/identity/` (`createIdentityUser()`, a working validated-create + role-lookup + transactional-audit example, smoke-tested end to end and cleaned up). Seeded three `Role` rows only (Administrator, Principal, Teacher) — no `User` rows, per this sprint's explicit instruction. No API, UI, or business module built. (Its `src/validators/` folder was later consolidated into the pre-existing, general `src/lib/validations/` — see [D-029](./DECISIONS.md#d-029--final-identity-architecture-review-validators-consolidated-into-libvalidations-roleiduseraccesslevel-design-affirmed).)
- **Sprint 2 — Academic Foundation** ([D-030](./DECISIONS.md#d-030--sprint-2-academic-foundation-schoolclass-naming-classsubject-deferred-repository-retrofit-for-schoolacademicyear)): Migration 003 — `SchoolClass` (named to avoid JS/TS's `class` reserved word), `Section`, `Subject`, applied for real to the live database. Retrofitted `School`/`AcademicYear` with real repositories (`src/repositories/{school,academicYear}/`) — Sprint 0's `seed.ts` had accessed them directly, before any repository layer existed — and refactored `seed.ts` to route every write through repositories/services, no longer constructing its own Prisma client. Added `src/repositories/{schoolClass,section,subject}/` and one orchestrating `src/services/academic/` (`createSchoolClassWithSections()`, `createAcademicSubject()`), both transactional and audit-logged, smoke-tested end to end and cleaned up. Seeded 11 generic classes (Nursery–Class 8) with sections A/B each, and 10 generic subjects — all sourced from real, general Indian-schooling conventions (`docs/domain/DOMAIN_MODEL.md`), not Pant-Public-School-specific facts; confirmed via grep that no repository/service/validator file references the school by name. `ClassSubject` (linking subjects to classes) deliberately not built — outside this sprint's explicit scope. No API, UI, Student, Teacher, or Attendance/Examination module built.
- **Sprint 3 — Student Foundation** ([D-031](./DECISIONS.md#d-031--sprint-3-student-foundation-dto-layer-introduced-admissionnumber-scope-fix-two-lifecycle-operations-instead-of-one-transaction-contention-bug-found-and-fixed)): Migration 004 — `Student`, `Guardian`, `StudentGuardian`, `Enrollment`, applied for real to the live database. The project's first DTO layer (`src/services/student/{student,guardian,enrollment}.dto.ts`) — every exported service function returns a DTO, confirmed by grep that no raw Prisma model crosses that boundary. Two lifecycle-oriented functions in `src/services/student/`: `registerStudent()` (identity) and `enrollStudent()` (the year-scoped fact) — kept separate, matching `EVENT_MODEL.md`'s own distinct `StudentEnrolled` event and the precise scope of this sprint's "Enrollment is the aggregate root for all academic history" decision (a root for academic history, not for `Student` itself). `src/repositories/{student,guardian,enrollment}/` (StudentGuardian's data access folded into the Student/Guardian repositories — no dedicated repository, per this sprint's own five-repository list). Corrected `Student.admissionNumber` to school-scoped uniqueness (`DATABASE_REVIEW.md § 2` flags the illustrative doc's global uniqueness as wrong). Found and fixed a real transaction-connection-contention bug — a read issued via the shared singleton while a `db.$transaction` was open, competing for a Neon connection — surfaced only by actually running the seed script's create path, not by any static check. Seeded 3 generic guardians and 5 generic students (two sibling pairs), all enrolled into Sprint 2's academic structure. No API, UI, Teacher, Attendance, Examination, Admission, Promotion, or Transfer Certificate module built.
- **Sprint 4 — Teacher Foundation** ([D-032](./DECISIONS.md#d-032--sprint-4-teacher-foundation-repository-dependency-rule-made-explicit-registerteacher-composes-createuser-updateteacherassignment-soft-deletes-rather-than-mutates)): Migration 005 — `Teacher`, `TeacherQualification`, `TeacherAssignment`, applied for real to the live database, completing the "Enrollment & assignment" migration slot Sprint 3 half-filled. `src/repositories/{teacher,teacherQualification,teacherAssignment}/` — confirmed by grep that no repository imports another repository, a rule now documented permanently in [docs/engineering/ENGINEERING_PRINCIPLES.md](./engineering/ENGINEERING_PRINCIPLES.md). `src/services/teacher/` — `registerTeacher()` (composes `createUser()` from the identity domain with `createTeacher()`/`createTeacherQualification()` in one transaction, per `TRANSACTION_BOUNDARIES.md`'s "Teacher onboarding" row), `assignTeacher()`, `updateTeacherAssignment()` (soft-deletes and replaces, never mutates a live assignment in place — preserves the historical record `DATABASE_REVIEW.md § 7` says future Attendance/Marks provenance depends on), `deactivateTeacher()`. First service to use a `dto/` subfolder (`teacher.dto.ts`, `teacherQualification.dto.ts`, `teacherAssignment.dto.ts`). Found and fixed a second, distinct cause of the "client already executing a query" warning — Prisma's own query-engine decomposition of `create()+include` inside an open transaction, not Sprint 3's connection-contention bug — for Teacher's own code; the same benign pattern was confirmed to pre-exist in Sprint 3's `enrollment.repository.ts`, deliberately not fixed there (out of this sprint's scope). Seeded 3 generic teachers with qualifications and assignments into Sprint 2's academic structure. No API, UI, Attendance, Timetable, Leave Management, Payroll, Examination, or Notifications module built.
- **Delivery Phase Roadmap Reorganization** ([D-034](./DECISIONS.md#d-034--delivery-phase-roadmap-clone-per-client-model-supersedes-eventual-saas-framing-epic-reordering)): a planning sprint, not an implementation — organized all remaining work into product Epics under the explicit clone-per-client delivery model (one Master Repository, cloned into an independent repository per client school, never a shared multi-tenant SaaS deployment), superseding `PRODUCT_ARCHITECTURE.md § 2` step 4's and `ROADMAP_V2.md`'s prior "eventual SaaS" framing via a surgical cross-link, not a rewrite. Produced [docs/product/](./product/README.md): a reordered Epic sequence, a complete Administration lifecycle design, a reusable Import Engine design, officially-sourced research confirming no supported integration exists for Rajasthan's RTE portal, a corrected client delivery playbook, and a framework/versioning strategy. No code, schema, migration, or service touched.
- **Sprint B1 — Authentication Foundation** ([D-035](./DECISIONS.md#d-035--sprint-b1-authentication-foundation-jwt-session-strategy-corrects-d-030-empirically-confirmed-incompatible-with-credentials-only-argon2id-in-libsecurity-auth-as-its-own-service-adminteacher-route-groups-renamed-to-real-path-segments)): the first real slice of Epic B — Auth.js Credentials authentication (`src/lib/auth/`), Argon2id password hashing (`src/lib/security/`), `src/services/auth/` (`authenticateUser()`, `resolveActiveSessionUser()`), login/logout, and two-tier route protection (`src/middleware.ts` Edge pre-check + `src/app/admin/layout.tsx`/`src/app/teacher/layout.tsx` Node-runtime authoritative check). Corrected D-030's "database sessions" decision to JWT after empirically confirming `@auth/core` hard-rejects that combination with Credentials-only — the underlying instant-revocation requirement is preserved via a live database read in the `session` callback on every request, verified end to end (a mid-session deactivation cuts off an already-valid session on its very next request). Found and fixed a pre-existing scaffold bug: `(admin)`/`(teacher)` route groups (no URL segment) would have collided with `ROUTES.md`'s own documented `/admin/*`/`/teacher/*` paths — renamed to real path-segment folders while still empty. Seeded a Bootstrap Administrator with obvious placeholder credentials, never through an HTTP endpoint. No User Management, Dashboard, Create User UI, Reset/Forgot Password, or Student/Parent login built.
- **Sprint B2 — Bootstrap Administration & User Management Foundation** ([D-036](./DECISIONS.md#d-036--sprint-b2-administration--user-management-foundation-usermustchangepassword-column-centralized-libauthorization-orchestrated-not-modified-registerteachercreateidentityuser-cookie-flashed-temporary-passwords)): Migration 007 — `User.mustChangePassword` (purely additive). `src/lib/authorization/` centralizes every permission check (`canManageUsers`/`canManageTeachers`/`canResetPasswords`/`canDeactivateUsers`, `requireSession()`/`requirePermission()`) — every administrative Server Action calls it exactly once, no scattered inline `accessLevel === "ADMIN"` comparisons. `src/services/administration/` — `createUserAccount()` (branches on Role.accessLevel, orchestrates the unmodified `createIdentityUser()`/`registerTeacher()`), `editUserAccount()` (never own role), `deactivateUserAccount()`/`activateUserAccount()` (never self-deactivate), `resetUserPassword()`, `changeOwnPassword()` (the only path clearing `mustChangePassword`, requires current-password confirmation), `searchUserAccounts()`. A shared `applyTemporaryPassword()` generates a CSPRNG-backed, ambiguous-character-free 12-character password (`generateTemporaryPassword()`, `src/lib/security/password.ts`) for both account creation and Admin-initiated reset. `src/app/admin/users/` (list/search/filter, create, details, edit, reset password) and top-level `/change-password` built as plain Server Component forms — the two flows needing a one-time-display secret (create, reset) were initially built with `useActionState` Client Components, then rebuilt after real end-to-end verification found that protocol unverifiable in this environment (no headless-browser tool available; `curl` simulation failed on bound Server Actions with "Missing origin header"/"Failed to find Server Action"); the replacement flashes the temporary password via a short-lived (60s), `httpOnly` cookie on redirect to the Details page instead — verified working end to end for Create Teacher, Create Administrator, and Reset Password. Every mutation writes an `AuditLog` entry, confirmed via direct query (`CREATE`/`UPDATE`/`SOFT_DELETE`, never a password hash or plaintext). `docs/development/DEVELOPMENT_LOGIN.md` documents the Bootstrap Administrator's login/rotation, resolving Sprint B1's Open Question #1; a dedicated review of the auth implementation for hardcoded values/duplicated logic found none requiring a fix, resolving Open Question #2. No Student/Guardian management, Attendance UI, Dashboard polish, Notifications, Forgot Password, or Email integration built.
- **Sprint B2.1 — Authentication Stabilization** ([D-037](./DECISIONS.md#d-037--sprint-b21-authentication-stabilization-root-cause-was-stale-live-credentials-not-a-code-defect)): traced a reported login failure to stale live credentials left over from this project's own prior end-to-end verification pass — no code defect found. Fixed by resetting the affected row through the existing `updateUserPassword()`/`writeAuditLog()` path, not a raw SQL edit or a re-seed.
- **Sprint B2.2 — Post-Login Redirect Fix** ([D-038](./DECISIONS.md#d-038--sprint-b22-post-login-redirect-single-resolvepostloginredirect-resolver-replaces-three-independent-role-mapping-implementations-auth-does-not-see-a-same-request-signinredirectfalse-cookie-in-this-nextjs-version)): a real defect — login defaulted to the public `/` homepage instead of `/change-password` or a role landing page. Fixed with one authoritative `resolvePostLoginRedirect()` (`src/lib/authorization/redirect.ts`), replacing three independent role-mapping implementations (the login action, `change-password/actions.ts`'s own inline ternary, and what the layouts' `mustChangePassword` check implicitly assumed).
- **Sprint B3 — First-Time Setup Wizard & System Readiness** ([D-039](./DECISIONS.md#d-039--sprint-b3-first-time-setup-wizard--system-readiness-frameworkconfig-as-a-write-once-snapshot-not-a-live-cache-one-checksystemreadiness-service-reused-by-three-consumers-middleware-x-pathname-header-for-a-server-component-loop-safe-redirect)): Migration 008 — `FrameworkConfig`, a write-once "what was true when setup completed" snapshot (`frameworkVersion`/`databaseVersion`/`migrationVersion`/`setupCompleted`/`setupCompletedAt`/`setupCompletedBy`) — deliberately not a live-state cache. `checkSystemReadiness()` (`src/services/system/`) computes Database/Schema/Bootstrap/Roles/School/AcademicYear/Authentication/Version/Overall fresh on every call, reused unmodified by three consumers: the new `/admin/setup` Setup Wizard (System Verification, School Verification with real edit capability for the first time, Bootstrap Verification linking to the existing password-reset flow, Finalize Setup), a rebuilt Admin Home (`/admin`, replacing Sprint B1/B2's bare link-list stub with System Ready/Framework Version/Current School/Current Academic Year/Current User + Quick Actions), and the new `/admin/system` Developer Information page. `src/app/admin/layout.tsx` redirects every `/admin/*` route to `/admin/setup` until setup is finalized, using a small additive `x-pathname` header `src/middleware.ts` now sets (no new database read on the Edge runtime) so the Server Component guard can avoid redirecting the wizard to itself. Verified end to end on a live production build, including a genuine "second startup skips the wizard" pass. Named, not fixed: the generic Nursery–8 class/section/subject seed list is likely also a dev-fixture not meant for real client deployments — flagged for Epic C, out of this sprint's own scope.
- **Sprint B4 — Audit Log Viewer & Administration Freeze** ([D-040](./DECISIONS.md#d-040--sprint-b4-audit-log-viewer--administration-freeze-writeauditlog-now-delegates-to-a-real-repository-closing-a-sprint-0-era-exception-exact-match-redaction-denylist-replaces-a-substring-match-that-redacted-its-own-safe-audit-flags-epic-b-declared-complete)): `/admin/audit` and `/admin/audit/[id]` — Administrator-only, Server Components, GET-based filters (Date, Action, Entity Type, Actor, Search), pagination. New `src/repositories/auditLog/` and `src/services/audit/` (`searchAuditEvents()`, `getAuditEvent()`, `listEntityTypeOptions()`). Fixed a genuine architectural inconsistency found during this sprint's own Administration Architecture Review: `writeAuditLog()` (Sprint 0) called Prisma directly, the one model with no repository to route through until this sprint built one — now delegates to `createAuditLog()`, signature unchanged, zero call-site edits across ~27 existing usages. Found and fixed a real redaction bug during verification (a substring match over-redacted the safe `passwordReset`/`mustChangePassword` audit flags; replaced with an exact-match denylist). Bootstrap Administrator and Security reviews confirmed no other inconsistencies. **Epic B declared complete (8/8).**
- **Sprint C1 — Branding & Configuration Framework** ([D-041](./DECISIONS.md#d-041--sprint-c1-client-configuration-framework-school-db-extended-as-the-admin-facing-source-of-truth-srcconfigschoolts-remains-the-separate-public-website-source-named-divergence-not-closed-this-sprint-branding-color-boundary-reaffirmed-as-deliberate)): `/admin/configuration` — Administrator-only, School Identity + Academic fields editable (Migration 009 — `School` extended additively with 11 new nullable columns), Theme/Website/Social shown read-only with a note pointing to the `src/config/*.ts` file to edit. New `src/services/configuration/` (Epic C, deliberately separate from Epic B's frozen `system.service.ts`, reusing the same `School`/`AcademicYear` repositories directly — zero repository changes needed, both were already generic). Architecture Review of `src/config/` found `src/config/` already holds zero "Daily School Data," and surfaced one real, honestly-named-not-closed finding: `School` (DB) and `SCHOOL` (`src/config/school.ts`, static) are two unsynced sources for overlapping facts — closing this would mean redesigning the public website's rendering pipeline, out of scope. Branding Review: logo/favicon are now genuinely client-configurable (URL fields); the primary color's code-only boundary was reaffirmed as deliberate (`CONFIGURATION_GUIDE.md § 1`), not fixed.
- **Sprint C2 — Client Onboarding Certification** ([D-042](./DECISIONS.md#d-042--sprint-c2-client-onboarding-certification-readmemd-quick-start-was-genuinely-onboarding-blocking-missing-migrateseedlogin-steps-prismaseedts-gains-a-seed_dev_fixtures-flag-closing-a-real-client-deployment-gap-live-dry-run-performed-against-a-temporarily-reset-deployment-state)): a Business Workflow Review and a live dry run (not code review alone) against a temporarily-reset deployment state, certifying whether a new engineer can actually onboard a school using only the repository and docs. Found and fixed a genuinely onboarding-blocking stale `README.md` (Quick Start missing the migrate/seed/login sequence entirely). Added a `SEED_DEV_FIXTURES` environment flag to `prisma/seed.ts`, closing a real gap between what `CLIENT_IMPLEMENTATION_PLAYBOOK.md`/`GO_LIVE_CHECKLIST.md` already required (reference-data-only seeding for a real client) and what the seed script actually supported. Produced [docs/product/ONBOARDING_CERTIFICATION_CHECKLIST.md](./product/ONBOARDING_CERTIFICATION_CHECKLIST.md) — a reusable checklist for the next real client's onboarding.
- **Sprint D1 — Import Engine Architecture & Foundation** ([D-043](./DECISIONS.md#d-043--sprint-d1-import-engine-architecture--foundation-two-new-tables-importbatchimportrow-each-independently-justified-chunked-commit-is-one-transaction-per-row-not-per-chunk-matching-transaction_boundariesmd--4s-rollover-precedent-entity-agnostic-extension-points-only--no-real-importer-built)): Migration 010 (`ImportBatch`/`ImportRow`) and a fully entity-agnostic orchestration service (`src/services/import/`) covering Upload → Detect → Map → Validate → Preview → Commit → Audit → History → Report — foundation only, no `StudentImporter`/`TeacherImporter`/entity-specific importer built. Commit is resumable and per-row-transactional (one transaction per row, not per chunk, per `TRANSACTION_BOUNDARIES.md § 4`'s Rollover precedent), verified live via a scratch script proving chunking, resumability, and per-row commit failure isolation. `/admin/imports` is a minimal, correctly-empty History list.
- **Sprint D2 — Academic Structure Importer** ([D-044](./DECISIONS.md#d-044--sprint-d2-academic-structure-importer-first-real-importer-built-as-the-reference-architecture-two-genuine-gaps-found-in-sprint-d1s-own-foundation-and-fixed-importrowvalidator-widened-async-academicservicets-gains-transaction-passthrough-importentitytype-simplified-to-one-academic_structure-value)): the first real Import Engine importer (`src/services/import/importers/academicStructure/`) — row grouping, a column alias dictionary, Data Profiling (`src/services/import/profiling.ts`, generic and reusable), Business+Database Validation, and a commit handler reusing `createSchoolClassWithSections()`/`createAcademicSubject()` unchanged. Found and fixed two real gaps in Sprint D1's own foundation: `ImportRowValidator` widened async; `academic.service.ts` gained transaction passthrough, closing a real idempotency bug on commit retry. `ImportEntityType` simplified to one `ACADEMIC_STRUCTURE` value. Verified live with real (not synthetic) entity creation, confirmed by direct database query and by the reused services' own normal `AuditLog` entries firing.
- **Sprint D3 — Upload, Detection & Smart Mapping** ([D-045](./DECISIONS.md#d-045--sprint-d3-upload-detection--smart-mapping-two-new-dependencies-papaparseexceljs-import-profiles-formalize-d2s-ad-hoc-aliases-into-a-reusable-registry-a-live-found-next_redirect-swallowed-by-trycatch-bug-fixed-in-the-mapping-action)): `/admin/imports/{upload,mapping,preview}` — CSV/XLSX parsing (`papaparse`/`exceljs`), deterministic Import Type Detection, reusable Import Profiles + alias dictionaries (`src/services/import/profiles.ts`), Saved Mapping Template reuse, an Import Health Summary/Quality Score on top of `buildDataProfile()`. Reuses D1/D2's Preview/Commit pipeline unchanged. Found and fixed a real bug via live HTTP testing (not code review): the Mapping page's success `redirect()` was inside its own `try/catch`, mistaking Next.js's internal redirect throw for a real error on every successful submission. Verified via both a service-layer scratch script and a real HTTP walkthrough of the actual pages, producing a real committed `SchoolClass`.
- **Sprint D4 — Import Engine Freeze & Architecture Certification** ([D-046](./DECISIONS.md#d-046--sprint-d4-import-engine-freeze--architecture-certification-16-question-certification-against-real-code-not-assumed-one-genuine-gap-found-and-fixed-studentteacherattendanceservicets-gain-the-same-tx-passthrough-d2-gave-academicservicets-verified-live-with-a-real-rollback-test-two-narrower-composability-constraints-named-but-deliberately-not-fixed-epic-ds-core-engine-declared-frozen)): 16 mandatory architecture questions certified against real code. Confirmed D-044's own prediction — `student.service.ts`/`teacher.service.ts`/`attendance.service.ts` lacked the `tx`-passthrough `academic.service.ts` already had — fixed identically for `registerStudent()`/`enrollStudent()`/`registerTeacher()`/`deactivateTeacher()`, plus a deeper fix for `assignTeacher()`/`updateTeacherAssignment()`/`openAttendanceSession()`/`markAttendance()`/`submitAttendance()`, which previously read their own just-written row back only after their transaction committed — now read via the same still-open transaction. `src/repositories/{teacherAssignment,attendanceSession,attendanceRecord}/`'s three `findXById()` functions gained an optional `tx` parameter to support this. Verified live via a scratch script (not typecheck alone): an externally-provided transaction, forced to roll back, proved a student genuinely wasn't left half-committed; a teacher-plus-assignment composed into one still-open transaction proved the read-via-tx fix actually works. That same test surfaced two narrower, real, honestly-named-not-fixed constraints (a pre-transaction existence check that can't see a same-transaction uncommitted row; a pre-check that still reads via the plain `db` singleton, reproducing D-031's already-named connection-contention warning) — real future plumbing for a Teacher/Attendance importer that doesn't exist yet, not urgent today. Q4/Q5 (Results/Admission import): confirmed via schema grep that no `Examination`/`MarksRecord`/`Admission*` tables exist — a data-model gap belonging to Epic E/F, not this engine. **Epic D's core engine is now formally frozen.**
- **Sprint E0 — Student Domain Architecture & Workspace Certification** ([D-047](./DECISIONS.md#d-047--sprint-e0-student-domain-architecture--workspace-certification-15-question-certification-against-real-code-and-domain-docs-a-genuine-createinclude-decomposition-risk-found-and-fixed-in-enrollmentrepositoryts-mirroring-d-046-a-real-epic_roadmapmd-gap-promotionrecordtransfercertificate-had-no-epic-home-fixed-student-architecture-declared-frozen)): 15 mandatory architecture questions certified against real code and the published domain documentation — no Student UI, CRUD, import, or Timeline built. Found and fixed a genuine gap mirroring D-046's own methodology exactly: `enrollment.repository.ts`'s `createEnrollment()` carried a `create()+include` in one call — a previously-named-but-deferred issue (D-032) that became a live risk once D-046 gave `enrollStudent()` an externally-supplied `tx`. Fixed identically (drop the `include`, `findEnrollmentById()` gains optional `tx`, read back via the same still-open transaction). Live-verified with a self-cleaning scratch script, which surfaced an honest correction to D-046's own prior conclusion — Student Import is not fully unconstrained after all; `registerStudent()` + `enrollStudent()` composed into one transaction hits the identical "pre-transaction existence check can't see a same-transaction uncommitted row" constraint D-046 already found for Teacher/Attendance, confirmed live. Also found and fixed a real, independent `EPIC_ROADMAP.md` gap: `PromotionRecord`/`TransferCertificate` were fully specified in the domain docs but assigned to no Epic — slotted into Epic E's own Implementation Order after Examination. Q1 (aggregate root): `Student` and `Enrollment` are two separate, cooperating aggregates, not one owning the other — `Student` is the durable-identity root, `Enrollment` the temporal-fact hub; a repository comment overstating this as "Enrollment is the aggregate root" was corrected to "the temporal hub." Student Workspace, Timeline, Search, and a Student permission module are all certified as buildable without further architecture change — none built this sprint. **The Student domain architecture is now formally frozen.**
- **Sprint E1 — Student Workspace ("Student 360")** ([D-048](./DECISIONS.md#d-048--sprint-e1-student-workspace-student-360-first-real-student-facing-capability--directory-searchfiltersortpagination--profile-read-composition-workspace-zero-new-tables-zero-frozen-architecture-changes-verified-live-via-a-minted-session-against-real-seeded-data)): the first real, user-facing Student capability — `/admin/students` (Directory: search across Student/Guardian/Enrollment, Status/Class/Section/Academic Year filters, sort, pagination, mirroring `searchUsers()`/`searchAuditLogs()`'s own established shape) and `/admin/students/[id]` (Profile/"Student 360": a pure read-composition Workspace — Identity, Current Enrollment, Guardian Summary, Attendance Summary, honest placeholder states for Academic Snapshot/Recent Activity/Documents, permission-gated Quick Actions). No new database table — the Workspace composes repositories this codebase already had, plus one narrowly-scoped addition (`countAttendanceByEnrollment()`). Four new, distinct Admin-only permissions (`canViewStudents`/`canManageStudents`/`canTransferStudents`/`canDeactivateStudents`) ready for a future scoped Teacher view without touching the other three. Student CRUD is explicitly not built — six of seven Quick Actions render disabled with an honest reason; Import History is a genuinely real link to the frozen, working Import Engine, not a placeholder dressed up as one. Verified live: a valid session minted directly via `next-auth/jwt`'s own `encode()` (sidestepping this project's previously-documented bound-Server-Action `curl` friction), exercised against real seeded data — guardian-phone search matching two siblings, an empty-result query, an ALUMNI-filter empty state, descending admission-number sort, a real student's full Profile render, a 404 for a bad id, and a 307-to-`/login` for no session, all confirmed working.
- **Sprint E2 — Attendance Operations Workspace** ([D-049](./DECISIONS.md#d-049--sprint-e2-attendance-operations-workspace-the-first-real-teacher-facing-daily-workflow--attendance-home-session-grid-and-history-composed-entirely-from-sprint-5s-own-unmodified-submitattendanceopenattendancesession-session-locking-implemented-as-derived-state-no-schema-change-a-user-clarified-late-vs-leave-wording-question-resolved-without-touching-the-existing-enum)): the first genuinely daily, teacher-facing workflow — `/teacher/attendance` (Home), `/teacher/attendance/[sectionId]` (Session — an interactive Grid, this codebase's first Client Component), `/teacher/attendance/history` (read-only). Composes Sprint 5's own unmodified `openAttendanceSession()`/`submitAttendance()` completely unchanged; zero new tables. Session "locking" (every enrolled student marked) is derived at read time, not a stored column, with a live-verified extension point (`canOverrideAttendanceLock()`) for a future override capability. A genuine "Late" vs. the schema's real `LEAVE` status wording ambiguity in the task prompt was resolved by asking the Project Owner directly, not guessed — kept as `LEAVE`, no enum change. Class-Teacher-only attendance-rights scoping (not every subject teacher) enforced server-side on every read and submit. Verified live: a real submission correctly locked a session, a second submit was correctly rejected without override and accepted with it, and Sprint E1's Student Workspace Attendance Summary correctly reflected the new submission with zero code changes to it.
- **Sprint E3 — Teacher Workspace ("Teacher 360")** ([D-050](./DECISIONS.md#d-050--sprint-e3-teacher-workspace-teacher-360-the-first-teacher-self-service-home-screen--replaces-the-sprint-b1-placeholder-stub-composes-every-prior-epic-e-sprints-own-services-unchanged-canviewstudents-extended-to-admit-teacher-the-extension-d-048-named-in-advance-searchstudents-gains-a-hard-server-side-section-scope-boundary)): the capstone of Epic E's three-sprint Teacher-facing arc — `/teacher` (Dashboard, replacing the Sprint B1 placeholder: identity, assignments, today's Attendance state via `getAttendanceHome()`/`searchAttendanceHistory()` reused unchanged from Sprint E2, Students Under Care, Student Quick Access, Recent Activity placeholder, Quick Actions), `/teacher/students` and `/teacher/students/[id]` (reusing `searchStudentDirectory()`/`getStudentWorkspace()` from Sprint E1 completely unchanged). Zero new tables. `canViewStudents()` extended to admit `TEACHER` — the exact extension D-048 named in advance — while the three Student mutation permissions stay Admin-only. `searchStudents()` gained an additive `scopedToSectionIds` hard boundary, used only by the new Teacher pages; `getStudentWorkspace()` itself was not modified — the Teacher-facing profile page instead checks the returned student's section against the caller's own assignments after calling it, `notFound()` otherwise. Verified live: correct identity/assignments/attendance-state/quick-access rendering for a real Class Teacher, a correctly-enforced 404 for a student outside her sections, and an Admin regression check confirming `/admin/students` remains unaffected by the permission broadening.
- **Sprint E4 — Principal Workspace ("Principal 360")** ([D-051](./DECISIONS.md#d-051--sprint-e4-principal-workspace-principal-360-a-school-operations-dashboard-enhancing-the-existing-admin-home-in-place-rather-than-a-parallel-route--principal-and-administrator-are-both-accesslevel-admin-role-rows-d-028d-029-so-no-new-accesslevel-distinction-was-needed-one-shared-per-section-computation-feeds-both-attendance-and-teacher-overview)): Epic E's capstone — a School Operations Dashboard built by enhancing `/admin` (Sprint B3's own Admin Home) in place, not a parallel route, since Principal and Administrator already share `accessLevel: ADMIN` (D-028/D-029). `getPrincipalWorkspace()` (`src/services/principal/`) composes `checkSystemReadiness()`/`getSchoolDetails()` (Sprint B3), `getConfigurationSummary()` (Sprint C1), `listImportBatches()` (Sprint D1/D3), and `searchStudentDirectory()` (Sprint E1) completely unchanged, plus one new shared `computeSectionStates()` helper feeding both Attendance Overview and Teacher Overview from a single per-section computation (one Class Teacher per section, by design — not two figures to compute separately). Zero new database tables; the only new repository function is `listSectionsBySchoolAndYear()` (no prior function enumerated every section in a school for a year). Operational Alerts are entirely deterministic — no AI, no predictions — derived from already-computed signals, including one honestly-labeled Audit anomaly placeholder. `canViewPrincipalWorkspace()` is a named `accessLevel === "ADMIN"` check, identical to every other Admin-only permission in the codebase — Principal/Administrator have no distinct `AccessLevel` to check, so none was invented. Two Quick Actions ("Teachers," "Attendance") had no genuine existing destination and were resolved honestly rather than linked to a broken page — a pre-filtered `/admin/users?roleId=<Teacher role id>` and a same-page anchor to this Dashboard's own Attendance Overview section, respectively — instead of building new, out-of-scope pages. Verified live end to end: real, internally-consistent data across all four Overview sections, correctly-firing alerts, a Teacher session correctly blocked (307), and the pre-filtered Teachers link returning the exact same count as the Dashboard's own figure.
- **Sprint 5 — Attendance Engine** ([D-033](./DECISIONS.md#d-033--sprint-5-attendance-engine-openattendancesessionsubmitattendancemarkattendance-kept-as-three-distinct-operations-attendancesession-carries-its-own-schoolid-enrollment-section-scoping-enforced-in-the-service)): Migration 006 — `AttendanceSession`, `AttendanceRecord` — applied for real to the live database, the first genuinely high-volume table in the schema, using a time-ordered UUIDv7 primary key from this first migration. `src/repositories/{attendanceSession,attendanceRecord}/` — confirmed by grep that neither imports another repository. `src/services/attendance/` — `openAttendanceSession()` (idempotent get-or-create for a section+day), `markAttendance()` (single-record correction primitive), `submitAttendance()` (the real batch submission, matching `TRANSACTION_BOUNDARIES.md`'s "Daily attendance submission" row exactly — `AttendanceRecord` × N upsert + `AuditLog` × (N+1)), `reopenAttendance()` (a pure read, no separate open/closed status field exists to toggle). Every write path enforces "Attendance belongs to Enrollment" by checking the enrollment's `sectionId` matches the session's, per `BUSINESS_RULES.md § 4`/`WORKFLOWS.md § 2`. `AttendanceSession` carries its own `schoolId` directly, matching `Enrollment`'s Sprint 3 precedent, for the oversight-view composite index `DATABASE_REVIEW.md § 9` names. `upsertAttendanceRecord()` uses Prisma's `upsert`, not check-then-create, per `TRANSACTION_BOUNDARIES.md § 5`'s own recommendation. Confirmed via `--trace-warnings` that Sprint 4's `create()+include`-inside-transaction warning does not recur. Seeded 1 `AttendanceSession` with 2 real `AttendanceRecord` rows — Sprint 3's actual seed data tops out at 2 enrolled students in any single section, not the task's literal "five," documented rather than fabricated. No API, UI, Timetable, Leave Management, Notifications, Reports, or Examination module built.
- **Contact & Visit Experience** (Contact & Visit milestone) at `/contact`: office information, school timings, visit guidance, a map placeholder (no embedded map — cites `IMAGE_REGISTRY.md § IMG-013`), FAQ, and an admission enquiry CTA. Informational only, no submission form — forwards admission-related contact to the still-planned `/admissions/enquiry`, the same split already established by `Admissions` itself. Promoted `Admissions`'s page-local `DataTable` helper to the shared Marketing Section Library on its second real use ([D-023](./DECISIONS.md#d-023--datatable-promoted-from-page-local-helper-to-shared-component)). No navigation change needed — `"Contact"` was already present in `NAV_LINKS` since Phase 1A. Six previously-Latent cross-cutting content items (address, email, phone, emergency contact, office/visit hours) became Active for the first time.
- **Document Center Experience** (Document Center milestone) at `/documents`: admission/academic documents, mandatory public disclosures, school policies, a circulars-and-notices explanation, FAQ, and a contact CTA. Informational only, no submission form. The first page to reference any downloadable document — populated `docs/onboarding/DOCUMENT_REGISTRY.md` (13 items, `DOC-001`–`DOC-013`) for the first time, previously empty since the Content Readiness Framework's creation. Its four document categories render from a single data array via `.map()`, not hand-written per-category JSX, so a future category is one new array entry, not a page rewrite ([D-024](./DECISIONS.md#d-024--document-center-renders-categories-from-a-data-array-not-hand-written-jsx-blocks)) — the concrete answer to the task's "generic enough for future schools through configuration" instruction without building a configuration engine. No navigation change needed — `"Downloads"` was already present in `NAV_LINKS` since Phase 1A. No government document, policy, or filename was invented — every one of the 13 documents is a named, empty slot citing its own registry ID.

Neither the library nor the design system is a full product feature on its own — both are the foundation `/about`, `/admissions`, `/academics`, `/campus`, `/school-life`, `/contact`, and `/documents` now build on, and every future public page will too. No end-to-end product feature (admission enquiry submission, notices, attendance, etc.) is complete yet — see §6. Full dashboard: [FEATURE_STATUS.md](./FEATURE_STATUS.md).

---

## 6. Pending Features (Version 1 Scope)

### Guest (Public Website)

Browse marketing/informational site (design system, About, Admissions, and Academics Experiences built; Facilities content page pending) · submit admission enquiry (informational Admissions page built; the actual enquiry form at `/admissions/enquiry` is still pending) · view notices · view gallery · download documents · contact school

### Admin

**Built (Sprint B2):** manage users — create Administrator/Teacher accounts, edit, activate/deactivate, reset password, search/filter — see [DECISIONS.md § D-036](./DECISIONS.md#d-036--sprint-b2-administration--user-management-foundation-usermustchangepassword-column-centralized-libauthorization-orchestrated-not-modified-registerteachercreateidentityuser-cookie-flashed-temporary-passwords). Pending: manage students · manage teachers (teacher-domain profile fields, not accounts) · manage website content · attendance oversight · examinations · reports · school settings · dashboard polish

### Teacher

Dashboard · attendance (mark/view for assigned classes) · marks entry · student list (assigned classes only) · profile · leave request

**Explicitly out of scope for Version 1:** Fee management, library, hostel, payroll, inventory, transport management, homework module, parent portal, student portal. Full exclusion list and rationale: [PRODUCT_REQUIREMENTS.md § Out of Scope](./PRODUCT_REQUIREMENTS.md#4-out-of-scope). Approval process for ever introducing one of these: [PROJECT_GUARDRAILS.md](./PROJECT_GUARDRAILS.md#module-approval-process).

Full feature list with priority: [PRODUCT_REQUIREMENTS.md § Feature List](./PRODUCT_REQUIREMENTS.md#5-feature-list-version-1). Planned route inventory: [ROUTES.md](./ROUTES.md).

---

## 7. Folder Structure (Scaffolded Phase 0B.1 — populated through Sprint B3 First-Time Setup Wizard & System Readiness)

Condensed top-level view; application code lives under `src/` per [DECISIONS.md § D-007](./DECISIONS.md#d-007--src-directory-layout). The public site, its component library, the data foundation (Migrations 000-008), Auth.js Credentials authentication, User Management, and a first-time Setup Wizard are now built; Student/Teacher-profile management and dashboard polish remain unbuilt. Full breakdown with files and rationale: [ARCHITECTURE.md § Folder Structure](./ARCHITECTURE.md#2-folder-structure).

```
/
├── src/
│   ├── middleware.ts    # Edge-runtime route-protection pre-check (JWT signature only) — D-035; also sets an
│   │                      x-pathname request header (D-039), no new database read
│   ├── app/            # (public) (auth) route groups + real admin/ teacher/ path segments (renamed from
│   │                      route groups — D-035) + admin/setup/, admin/system/ (D-039) + admin/audit/ (D-040) +
│   │                      admin/configuration/ (D-041) + admin/imports/ (D-043, +upload/mapping/preview D-045) +
│   │                      admin/users/ (D-036) +
│   │                      change-password/ (D-036) + unauthorized/ + api/ + dev/playground (dev-only)
│   ├── components/     # ui/ (Shadcn — button/input/label/card, D-035) shared/ admin/ teacher/
│   │                      website/sections/ (marketing library — D-012) website/pages/ (page composites — D-016)
│   ├── config/          # school/branding/navigation/contact/social/seo — see D-018
│   ├── lib/            # auth/ (Auth.js config, D-035) security/ (Argon2id + generateTemporaryPassword(), D-035/D-036)
│   │                      authorization/ (centralized permission checks + guards + resolvePostLoginRedirect(),
│   │                      D-036/D-038) version.ts (getFrameworkVersion(), D-039) utilities, seo.ts,
│   │                      db.ts/db-utils.ts (+checkMigrationsApplied/getDatabaseVersion, D-039)/env.ts
│   │                      (Prisma — D-027); validations/ is THE canonical Zod-schema location, app-wide
│   │                      (identity.ts, academic.ts, student.ts, teacher.ts, attendance.ts, auth.ts,
│   │                      administration.ts, setup.ts) — see D-029, D-030, D-031, D-032, D-033, D-035, D-036, D-039
│   ├── repositories/    # data-access layer — user/ (+reactivateUser/updateUserPassword/searchUsers/
│   │                      findFirstActiveAdminUser, D-036/D-039) role/ school/ (+findFirstSchool/updateSchool,
│   │                      D-039) academicYear/ (+updateAcademicYear, D-039) schoolClass/ section/ subject/
│   │                      student/ guardian/ enrollment/ teacher/ teacherQualification/ teacherAssignment/
│   │                      attendanceSession/ attendanceRecord/ frameworkConfig/ (D-039) auditLog/ (D-040,
│   │                      also owns the createAuditLog() write side lib/db-utils.ts's writeAuditLog() now
│   │                      delegates to) importBatch/ importRow/ (+findNextChunkToCommit() the resumability
│   │                      query, D-043) — no direct Prisma outside this folder; no repository calls another
│   │                      (PrismaAdapter(db) in lib/auth is the one narrow, named exception, scoped only to
│   │                      Account/Session/VerificationToken —
│   │                      see D-028, D-030, D-031, D-032, D-033, D-035 and
│   │                      docs/engineering/ENGINEERING_PRINCIPLES.md § 9)
│   ├── services/        # business logic — identity/, academic/, student/ (first DTO layer, D-031), teacher/
│   │                      (registerTeacher(), assignTeacher(), updateTeacherAssignment(), deactivateTeacher() — dto/
│   │                      subfolder, D-032), attendance/ (openAttendanceSession(), markAttendance(),
│   │                      submitAttendance(), reopenAttendance() — dto/ subfolder, D-033), auth/
│   │                      (authenticateUser(), resolveActiveSessionUser() — dto/ subfolder, D-035), administration/
│   │                      (createUserAccount(), editUserAccount(), deactivate/activateUserAccount(),
│   │                      resetUserPassword(), changeOwnPassword(), searchUserAccounts() — orchestrates
│   │                      identity/teacher services rather than modifying them, dto/ subfolder, D-036), system/
│   │                      (checkSystemReadiness(), getSchoolDetails()/updateSchoolDetails(),
│   │                      getBootstrapAdminDetails(), isSetupComplete(), completeSetup() — dto/ subfolder, D-039),
│   │                      audit/ (searchAuditEvents(), getAuditEvent(), listEntityTypeOptions() — dto/
│   │                      subfolder, redacts by an exact-match denylist, D-040), configuration/
│   │                      (getSchoolConfiguration(), updateSchoolConfiguration(), getConfigurationSummary() —
│   │                      dto/ subfolder, D-041), import/ (createImportBatch(), ingestImportRows(),
│   │                      validateImportBatch(), previewImportBatch(), skipInvalidRows(),
│   │                      commitImportBatchChunk() — one transaction per row, not per chunk —
│   │                      getImportReport(), listImportBatches(), getImportBatchDetail() — dto/ subfolder,
│   │                      extension-points.ts [ImportRowValidator (async, D-044)/ImportRowCommitHandler],
│   │                      profiling.ts [buildDataProfile(), generic, D-044], entity-agnostic, D-043/D-044;
│   │                      importers/academicStructure/ [the reference importer — rows.ts, aliases.ts,
│   │                      validator.ts, commitHandler.ts, profile.ts, importer.ts — D-044])
│   ├── hooks/          # shared custom hooks
│   └── types/          # shared TypeScript types (next-auth.d.ts session augmentation, +mustChangePassword — D-035/D-036)
├── prisma/             # schema.prisma (AuditLog, School, AcademicYear, Role, User (+mustChangePassword, D-036),
│                          Account, Session, VerificationToken, SchoolClass, Section, Subject, Student, Guardian,
│                          StudentGuardian, Enrollment, Teacher, TeacherQualification, TeacherAssignment,
│                          AttendanceSession, AttendanceRecord, FrameworkConfig (D-039), ImportBatch, ImportRow
│                          (D-043), ImportBatch +rawFileData (Migration 012, D-045)), migrations/ (000-012,
│                          D-041/D-043/D-044/D-045), seed.ts (+ Bootstrap Administrator, D-035/D-036 — does not
│                          touch FrameworkConfig, D-039; generic dev fixtures gated behind SEED_DEV_FIXTURES,
│                          D-042) — see D-027, D-028, D-030, D-031, D-032, D-033, D-035, D-036, D-039, D-042,
│                          D-043, D-044, D-045
├── docs/               # this documentation set, engineering/ (cross-cutting engineering rules — D-032, D-035),
│                          development/ (DEVELOPMENT_LOGIN.md — D-036)
└── public/             # static assets
```

This is a **feature-based, role-segmented** structure. `admin/`/`teacher/` real path segments (not route groups, since D-035) isolate Admin and Teacher surfaces from the public site both logically and visually.

---

## 8. Tech Stack

| Layer                | Choice                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Runtime              | Node.js 22 (pinned via `.nvmrc` and `package.json engines`)                                                                                                                                                                                                                                                                                                                                                                     |
| Frontend Framework   | Next.js 15 (App Router, `src/` layout)                                                                                                                                                                                                                                                                                                                                                                                          |
| UI Library           | React 19                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Language             | TypeScript (strict)                                                                                                                                                                                                                                                                                                                                                                                                             |
| Styling              | Tailwind CSS v4                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Component Library    | Shadcn UI                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Animation            | Framer Motion                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Icons                | Lucide Icons                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Forms                | React Hook Form                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Validation           | Zod                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Backend              | Next.js API (Route Handlers)                                                                                                                                                                                                                                                                                                                                                                                                    |
| Database             | PostgreSQL                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ORM                  | Prisma (`@prisma/adapter-pg` driver adapter; 20 models across Migrations 000-006 — `AuditLog`, `School`, `AcademicYear`, `Role`, `User`, `Account`, `Session`, `VerificationToken`, `SchoolClass`, `Section`, `Subject`, `Student`, `Guardian`, `StudentGuardian`, `Enrollment`, `Teacher`, `TeacherQualification`, `TeacherAssignment`, `AttendanceSession`, `AttendanceRecord`; see D-027, D-028, D-030, D-031, D-032, D-033) |
| Auth                 | Auth.js v5 (`next-auth@beta`) — Credentials provider, JWT session strategy, Argon2id hashing (`@node-rs/argon2`), `PrismaAdapter` (`@auth/prisma-adapter`) scoped to `Account`/`Session`/`VerificationToken` only; `src/lib/auth/` built, Sprint B1 (D-035). No self-registration, no OAuth provider configured.                                                                                                                |
| Internationalization | next-intl (installed only; routing not activated — see [DECISIONS.md § D-008](./DECISIONS.md#d-008--phase-0b1-tooling--dependency-additions))                                                                                                                                                                                                                                                                                   |
| PWA                  | next-pwa (installed only; not wired — see D-008)                                                                                                                                                                                                                                                                                                                                                                                |
| Hosting              | Vercel                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Media                | Cloudinary                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Source Control       | GitHub                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Package Manager      | pnpm ≥10 (pinned via `package.json engines`)                                                                                                                                                                                                                                                                                                                                                                                    |
| Lint/Format          | ESLint + Prettier (`eslint-config-prettier`, `prettier-plugin-tailwindcss`)                                                                                                                                                                                                                                                                                                                                                     |
| Git Hooks            | Husky + lint-staged                                                                                                                                                                                                                                                                                                                                                                                                             |

This table is canonical — other documents should link here rather than restate it. No additional libraries without a recorded decision (rationale: [DECISIONS.md § D-004](./DECISIONS.md#d-004--tech-stack-selection), [D-008](./DECISIONS.md#d-008--phase-0b1-tooling--dependency-additions); process: [AI_RULES.md](./AI_RULES.md)).

---

## 9. Design System Summary

- **Premium, not decorative.** Restraint over ornamentation.
- **Calm software.** Minimal cognitive load — teachers use this between classes, not as their primary job.
- **Consistency over novelty.** One table style, one empty state, one modal pattern — used everywhere.
- **Motion with purpose.** Feedback, never decoration.
- **Accessible by default.** A baseline requirement, not a checklist item.

Full design bible (color, typography, spacing, components, motion, accessibility, dark mode): [UI_DESIGN_SYSTEM.md](./UI_DESIGN_SYSTEM.md). Voice, tone, and copy rules (headlines, CTAs, error/success messages): [CONTENT_GUIDELINES.md](./CONTENT_GUIDELINES.md).

---

## 10. Coding Standards Summary

- Strict TypeScript — no `any`, no implicit types
- Server Components by default; `"use client"` only when justified
- No duplicate logic — shared logic extracted to `/lib`
- No new dependencies without a recorded decision
- All UI built from Shadcn primitives
- Semantic, accessible markup required, not optional

Full naming conventions, import order, error handling, logging, and formatting rules: [DEVELOPMENT_CONVENTIONS.md](./DEVELOPMENT_CONVENTIONS.md). Behavioral rules for AI assistants: [AI_RULES.md](./AI_RULES.md).

---

## 11. Important Decisions

- Three roles only: Guest, Admin, Teacher (no Parent/Student login in v1)
- No Fee, Library, Hostel, Payroll, Inventory, Transport, or Homework modules in v1
- Server Components as the default rendering mode
- Shadcn UI as the only component primitive source
- No application code during Phase 0A; Phase 0A.1 was documentation-only
- Application code nests under `src/`; `prisma/`, `public/`, `docs/` stay at repo root (D-007)
- Phase 0B.1 scaffolding is infrastructure-only: no pages, no database models, no auth logic — `next-auth`, `prisma`, `next-intl`, `next-pwa` are installed but not wired (D-008)
- Project runtime pinned to Node 22 / pnpm ≥10 via `.nvmrc` and `package.json engines`; `@base-ui/react` and `class-variance-authority` removed as orphaned dependencies (D-009)
- A placeholder indigo-blue accent color introduced for `primary` (plus `success`/`warning`/`info`/`surface` tokens); explicitly swappable, revisit once real brand identity is provided (D-010)
- Public-site composites (Header, Footer, Hero, nav) live in a new `components/website/` folder, parallel to `components/admin`/`components/teacher` (D-011)
- Reusable marketing sections live in `components/website/sections/` (not a `src/features/` tree) — reviewed and kept in place to avoid two parallel organizational models across the codebase; per-component folders (types/constants/README/index) adopted for the new library (D-012)
- `components/` vs. `src/features/` independently re-reviewed one phase later (Phase 1B.1) — reaffirmed; a role folder graduates to feature-subgrouping only once it exceeds ~8 components across 2+ domains (D-013)
- `SectionDivider` simplified from 4 variants to 2 — `minimal`/`decorative` were unspec'd, had zero consumers, and rendered identically to `light` (D-014)
- A permanent, production-guarded `/dev/playground` route replaces the per-phase build-and-delete temporary preview page for manual component verification (D-015)
- Full pages are built as `components/website/pages/<Page>/` composites (content.ts/sections.ts/page.tsx/metadata.ts split), with the actual App Router route file reduced to a one-line re-export (D-016)
- Shared SEO logic (`buildPageMetadata()`/`buildPageJsonLd()`) extracted to `src/lib/seo.ts` once a second page needed the identical boilerplate; every page's `metadata.ts` now supplies only its own title/description/path (D-017)
- `/admissions` is the informational Admissions Experience, not the enquiry form — the form is planned separately at `/admissions/enquiry`
- Centralized configuration layer at `src/config/{school,branding,navigation,contact,social,seo}.ts` — school-identity/branding/nav/contact/social/SEO facts defined once, imported everywhere; `school.ts` is the root of the dependency graph (D-018)
- `/campus` added to `NAV_LINKS`; `/facilities` (previously only planned) is effectively superseded by `/campus`'s narrative treatment of the same ground
- Prisma 7 connection config splits across `prisma.config.ts` (Migrate, `DIRECT_URL`) and a driver adapter in `src/lib/db.ts` (runtime client, `DATABASE_URL`) — schema-level `url`/`directUrl` no longer exists in this Prisma version (D-027)
- `AuditLog` is Migration 000 (built first, no FK dependencies); `School`/`AcademicYear` are Migration 001 — no later migration implemented yet (D-027)
- `Role` is a lookup table (`name` + `accessLevel` enum), not a flat enum field on `User` and not a many-to-many join table — every V1 user has exactly one role (D-028)
- `User` is one table serving both Auth.js's adapter contract and this app's business fields; `roleId` is required — no public self-registration, every account is Admin-provisioned (D-028)
- `src/repositories/`, `src/services/` — a new data-access/business-logic layer; no direct Prisma usage outside `repositories/` (D-028)
- `src/lib/validations/` is the one canonical Zod-schema location app-wide; `src/validators/` (briefly introduced in Sprint 1) was consolidated into it (D-029)
- `User.roleId` (single FK to `Role`) affirmed as correct over a `UserRole` many-to-many join table — no V1 user needs more than one role, and the repository layer already absorbs the migration cost if that ever changes (D-029)
- The `Class` domain concept is implemented as `SchoolClass` in the real schema — `class` is a reserved word in JS/TS; every field/relation follows suit (`schoolClassId`) (D-030)
- `ClassSubject` (Subject↔SchoolClass many-to-many) deliberately not built in Migration 003 — outside that sprint's explicit scope, deferred to a future migration (D-030)
- `Student.admissionNumber` is `@@unique([schoolId, admissionNumber])`, not globally unique; `Student` has no `deletedAt` — `status` is the only lifecycle signal (D-031)
- `Enrollment.id` uses a time-ordered UUIDv7, not `cuid2` — it's the FK target every future high-volume child table (Attendance, Marks) will reference (D-031)
- `registerStudent()` and `enrollStudent()` are two separate lifecycle operations, not one combined function — `Enrollment` is the aggregate root for academic history specifically, not for `Student` itself (D-031)
- From Sprint 3 onward, services must return DTOs, never raw Prisma models — `src/services/student/*.dto.ts` is the first implementation; Sprint 1/2's services are not retroactively required to conform (D-031)
- Repositories never call other repositories — an already-informal rule since Sprint 1, made explicit and permanent in `docs/engineering/ENGINEERING_PRINCIPLES.md`; services may still compose multiple repositories, including across domains (D-032)
- `registerTeacher()` creates `User` + `Teacher` + `TeacherQualification`×N in one transaction, per `TRANSACTION_BOUNDARIES.md`'s "Teacher onboarding" row — a service calling repositories across domains, not a repository calling a repository (D-032)
- `updateTeacherAssignment()` soft-deletes and replaces rather than mutating a live assignment in place — mutating would corrupt the historical record future Attendance/Marks provenance depends on (D-032)
- `AttendanceSession` carries its own `schoolId` directly, even though it's transitively scoped via `Section` → `SchoolClass` → `School` — matches `Enrollment`'s Sprint 3 precedent, needed for the `(schoolId, date)` cross-section oversight-view index (D-033)
- `submitAttendance()` is a single batch transaction (N `AttendanceRecord` upserts + N+1 `AuditLog` writes), not N separate `markAttendance()` calls — looping the latter would turn one required atomic submission into N independent ones (D-033)
- Every `AttendanceRecord` write checks the enrollment's `sectionId` matches the session's `sectionId`, not just the academic year — "Attendance belongs to Enrollment" is enforced at the exact scope `BUSINESS_RULES.md`/`WORKFLOWS.md` describe (D-033)
- One Master Repository, cloned into an independent repository per client school — never a shared multi-tenant SaaS deployment; supersedes `PRODUCT_ARCHITECTURE.md § 2` step 4's/`ROADMAP_V2.md`'s prior "eventual SaaS" framing (D-034)
- Client Customization Framework moved earlier in the Delivery Phase sequence (parallel with Data Migration Engine, right after Administration) — both are cheap and block onboarding a second client, unlike the originally proposed later position (D-034)
- No official API or bulk-export exists for Rajasthan's RTE portal (`rajpsp.nic.in`) — the Import Engine's ordinary CSV/Excel upload path is the correct integration method, not scraping (D-034)
- JWT session strategy, not database sessions — `@auth/core` hard-rejects database sessions when Credentials is the only provider (confirmed against the library's own assertion source); instant revocation on deactivation is preserved via a live database read in the `session` callback on every request instead (D-035, corrects D-030)
- `PrismaAdapter(db)` in `src/lib/auth/config.ts` is a permanent, narrow, documented exception to "no direct Prisma outside repositories" — scoped only to `Account`/`Session`/`VerificationToken`, the adapter's own contract tables, never a business domain model (D-035, `ENGINEERING_PRINCIPLES.md § 9`)
- `admin/`/`teacher/` are real path segments, not route groups — renamed from `(admin)`/`(teacher)` this sprint, since a route group adds no URL segment and both would have collided on identical child paths (D-035)
- Login ID is `User.email` — no separate `loginId` field added to the frozen Identity schema (D-035)
- `User.mustChangePassword` (Migration 007) — one additive Boolean, set whenever an Admin sets a password on someone's behalf, cleared only by the user's own successful `/change-password`; enforced at the Node-runtime layout level, not in `src/middleware.ts` (D-036)
- `src/lib/authorization/` is the one canonical place a permission question is answered — every administrative Server Action calls `requirePermission()` exactly once, at the top; services trust an already-authorized `actorUserId` and never re-check (D-036)
- `createUserAccount()` orchestrates the unmodified `createIdentityUser()`/`registerTeacher()` rather than extending either's signature — composition over modification of a completed module (D-036)
- The two flows needing a one-time-display secret (Create User, Reset Password) use plain `<form action={fn}>` Server Actions with a short-lived `httpOnly` flash cookie, not `useActionState` Client Components — the latter's RSC action protocol proved unverifiable with the tools available in this environment; the cookie approach was verified end to end and is judged strictly simpler (D-036)
- A reported login failure traced to stale live credentials from this project's own prior testing, not a code defect — fixed by resetting the row, not by changing any code (D-037)
- `resolvePostLoginRedirect()` (`src/lib/authorization/redirect.ts`) is the single authoritative place a post-login/post-password-change destination is computed — replaces three independent role-mapping implementations; a same-request `auth()` read immediately after `signIn({redirect:false})` does not see the just-set session cookie in this Next.js version, confirmed by direct testing, not assumed from source alone (D-038)
- `FrameworkConfig` (Migration 008) is a write-once "what was true at setup" snapshot, never a live-state cache — live facts are always recomputed fresh by `checkSystemReadiness()` on every call, one service reused by the Setup Wizard, Admin Home, and Developer Information (D-039)
- The Admin layout's setup-required redirect reads the current pathname via an `x-pathname` header `src/middleware.ts` now sets (no new database read) — the documented way a Server Component layout knows its own route without a client-only hook, avoiding a redirect loop on `/admin/setup` itself (D-039)
- `writeAuditLog()` (Sprint 0) now delegates to `src/repositories/auditLog/`'s `createAuditLog()` instead of calling Prisma directly — its own external signature is unchanged, so every existing call site needed zero edits (D-040)
- `AuditLog` redaction on read uses an exact-match key denylist, not a substring pattern — a substring match also redacted the safe `passwordReset`/`mustChangePassword` audit flags this codebase deliberately logs instead of a real secret (D-040)
- Epic B (Administration) is declared complete — 8/8 per `EPIC_ROADMAP.md`'s own Core+Full breakdown; Dashboard UI/polish was never part of its scope (D-040)
- `School` (DB) is extended, not duplicated into a new table, as the Admin-facing Client Configuration source — `School` is already the tenant root; a second table for the same class of fact would be a competing configuration system (D-041)
- `School` (DB) and `src/config/school.ts` (static, public-website-facing) are two currently-unsynced sources for overlapping facts — named as a real, unresolved gap, not silently closed; closing it means redesigning the public site's rendering pipeline, deferred to a future epic (D-041)
- Primary brand color stays code-only (`src/app/globals.css`), not configuration — reaffirmed deliberate per `CONFIGURATION_GUIDE.md § 1` (D-010, D-041), not a gap this sprint left open by oversight

Full decision log with alternatives considered and approval records: [DECISIONS.md](./DECISIONS.md).

---

## 12. Open Questions

- Exact brand identity (logo, color palette, fonts) — pending school input
- Admission enquiry form field list — pending school input
- Examination grading model: marks-based, grade-based, or both — pending school input
- Academic year start month and term structure — pending school input
- Expected student/teacher volume (affects pagination/reporting defaults) — pending school input

Full context on each: [PRODUCT_REQUIREMENTS.md § Open Product Questions](./PRODUCT_REQUIREMENTS.md#11-open-product-questions).

---

## 13. Current Risks

| Risk                                                                                                                                                                                                                                                                                                               | Impact                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Mitigation                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| No brand palette finalized                                                                                                                                                                                                                                                                                         | A placeholder indigo accent (D-010) now ships live on the public site; if the school's real brand color differs, every `primary`/`ring` reference updates from one token edit — but the visual identity itself isn't final                                                                                                                                                                                                                                                                                                                                                                                                                   | Token system makes the swap cheap by design; revisit `globals.css` `:root`/`.dark` values once real brand input arrives — tracked in [TASKS.md](./TASKS.md)                                                                                                                                       |
| Undefined content pipeline for public site copy/images                                                                                                                                                                                                                                                             | Hero/footer copy for Phase 1A is deliberately qualitative (no fabricated stats/facts, per [CONTENT_GUIDELINES.md § 12](./CONTENT_GUIDELINES.md#12-what-this-platform-never-says)); Academics/Facilities pages still need real content before they can be built                                                                                                                                                                                                                                                                                                                                                                               | Flagged in [TASKS.md](./TASKS.md); School Admin owns this dependency                                                                                                                                                                                                                              |
| `/about`, `/admissions`, `/academics`, `/campus`, `/school-life`, `/contact`, and `/documents` ship with explicit bracketed content placeholders, now also sourced from `src/config/{school,contact}.ts` (address, email, phone, principal's name, academic session, motto, emergency contact, office/visit hours) | Real, but honest — CONTENT_GUIDELINES forbids fabricating these facts, so the structure is built and correct, but nothing must go live in production with visible `[Placeholder — ...]` text                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Fully cataloged, not just flagged: [docs/onboarding/CONTENT_DASHBOARD.md](./onboarding/CONTENT_DASHBOARD.md) tracks every item with ID, priority, and status; [GO_LIVE_CHECKLIST.md](./onboarding/GO_LIVE_CHECKLIST.md) is the hard gate before any production deploy                             |
| Role-scoping enforcement discipline                                                                                                                                                                                                                                                                                | Teacher-to-class data scoping must be enforced server-side from the first line of backend code; easy to get wrong under time pressure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | [ARCHITECTURE.md § Security Principles](./ARCHITECTURE.md#7-security-principles) is mandatory reading before Phase 2                                                                                                                                                                              |
| Documentation drift                                                                                                                                                                                                                                                                                                | With this many documents, stale cross-references are a real risk                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | This file is the tie-breaker; [AI_RULES.md](./AI_RULES.md) requires documentation updates every phase                                                                                                                                                                                             |
| Machine default Node (v23.1.0) differs from the pinned project runtime                                                                                                                                                                                                                                             | `.nvmrc`/`package.json engines` now pin Node `>=22 <23`; a contributor on the machine's default Node gets a non-fatal `pnpm install` warning, and a genuinely fresh `prisma` package install specifically requires Node 22 in `PATH` (Prisma's own preinstall script hard-checks Node version at install time only — `prisma generate` and other commands run fine under any Node once installed)                                                                                                                                                                                                                                            | Resolved for this repo: pinned in `.nvmrc` + `package.json engines` (Phase 0B.1 Finalization); global `pnpm` upgraded to 10.34.5, which itself runs under any Node. Contributors should run `nvm use` (or equivalent) before their first install                                                  |
| `next-intl` and `next-pwa` installed but inactive                                                                                                                                                                                                                                                                  | Easy to forget these aren't actually wired in; a future session could assume i18n or PWA behavior exists when it doesn't                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Tracked explicitly in [DECISIONS.md § D-008](./DECISIONS.md#d-008--phase-0b1-tooling--dependency-additions) and [FEATURE_STATUS.md](./FEATURE_STATUS.md)                                                                                                                                          |
| Marketing Section Library components not yet used by any page (`StatisticsGrid`, `ImageText`, `ResponsiveImage`)                                                                                                                                                                                                   | Easy to forget they exist and rebuild similar patterns ad hoc when a Gallery page is eventually built; `About`, `Admissions`, `Academics`, `Campus`, `School Life`, `Contact`, and `Document Center` together now exercise 14 of the 17 components (`Prose` and `DataTable` both promoted to the library, see [D-020](./DECISIONS.md#d-020--prose-promoted-from-page-local-helper-to-shared-component)/[D-023](./DECISIONS.md#d-023--datatable-promoted-from-page-local-helper-to-shared-component)) — `ImageText`/`ResponsiveImage` are the natural fit for `Campus`'s, `School Life`'s, and `Contact`'s pending photography once it exists | [COMPONENT_INVENTORY.md § Marketing Section Library](./COMPONENT_INVENTORY.md#marketing-section-library) is the canonical list — check it before building any new public page section; browsable at the dev-only `/dev/playground` route ([ROUTES.md § 5](./ROUTES.md#5-development-only-routes)) |
| `/admissions`'s CTAs link to `/admissions/enquiry`, which doesn't exist yet                                                                                                                                                                                                                                        | Expected 404 until that route is built (same pattern already established for other nav links) — but easy to forget it's intentional, not a bug                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Tracked in [ROUTES.md § 1](./ROUTES.md#1-public-routes--public-route-group) and [TASKS.md](./TASKS.md)                                                                                                                                                                                            |
| `AuditLog`'s append-only grant-revocation and partitioning decision are both still deferred (Migrations 000-002 are now applied to a live Neon database as of Sprint 1, resolving the "no live database" half of this risk)                                                                                        | The append-only enforcement named in `AUDIT_STRATEGY.md § 2` needs a real Postgres role name that isn't chosen yet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Tracked in [D-027](./DECISIONS.md#d-027--sprint-0-data-foundation-migrations-000-001-implemented-prisma-7-config-model-schoolstatusseed-placeholders); apply both as a follow-up migration once a connection role is chosen                                                                       |
| `StatisticsGrid` built ahead of a real need (zero consumers, no confirmed data)                                                                                                                                                                                                                                    | Flagged in the Phase 1B.1 review as the library's weakest present-day justification — a future session could delete it prematurely or wire in unverified numbers                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | [DECISIONS.md § D-013](./DECISIONS.md#d-013--phase-1b1-architecture-review-reaffirmed-role-segmented-components-added-a-feature-subgrouping-threshold-rule) and [TASKS.md](./TASKS.md) record the reasoning — kept, not deleted, pending real statistics                                          |

---

## 14. Reference Links — Full Document Map

| Document                                                                         | Purpose                                                                                                                                           |
| -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| [PRODUCT_VISION.md](./PRODUCT_VISION.md)                                         | Platform-level mission, vision, audience, principles, and success metrics for the School Operating System                                         |
| [PRODUCT_ARCHITECTURE.md](./PRODUCT_ARCHITECTURE.md)                             | How the architecture is meant to evolve toward a reusable platform — current vs. future, boundaries                                               |
| [ROADMAP_V2.md](./ROADMAP_V2.md)                                                 | Epic-based forward roadmap (complements the phase-based [ROADMAP.md](./ROADMAP.md))                                                               |
| [CONFIGURATION_GUIDE.md](./CONFIGURATION_GUIDE.md)                               | Decision guide — what belongs in configuration, content, code, or a future database                                                               |
| [onboarding/README.md](./onboarding/README.md)                                   | Content Readiness Framework — registries, dashboard, collection guide, onboarding/go-live checklists                                              |
| [engineering/ENGINEERING_PRINCIPLES.md](./engineering/ENGINEERING_PRINCIPLES.md) | Cross-cutting backend layering rules (repository/service/DTO boundaries) established across Sprints 1-4, collected in one place — D-032           |
| [product/README.md](./product/README.md)                                         | Delivery Phase planning — Epic roadmap, Administration/Import Engine/Framework/Versioning strategy, client delivery lifecycle — D-034             |
| [domain/README.md](./domain/README.md)                                           | School Domain Model — entities, ERD, schema, business rules, workflows, permissions for Epic B (architecture only, not yet built)                 |
| [database/README.md](./database/README.md)                                       | Physical Database Review — indexing, constraints, enums, audit/soft-delete strategy, migration plan for Epic B (architecture only, not yet built) |
| [PRODUCT_REQUIREMENTS.md](./PRODUCT_REQUIREMENTS.md)                             | Full PRD — objectives, scope, exclusions, journeys, acceptance criteria                                                                           |
| [ARCHITECTURE.md](./ARCHITECTURE.md)                                             | System architecture, detailed folder structure, rendering/state/security/performance strategy                                                     |
| [UI_DESIGN_SYSTEM.md](./UI_DESIGN_SYSTEM.md)                                     | Design bible — color, typography, spacing, components, motion, accessibility, dark mode                                                           |
| [CONTENT_GUIDELINES.md](./CONTENT_GUIDELINES.md)                                 | Voice, tone, CTA/headline/error/success copy rules                                                                                                |
| [PROJECT_GUARDRAILS.md](./PROJECT_GUARDRAILS.md)                                 | Vision-protection rules and module approval process                                                                                               |
| [DEVELOPMENT_CONVENTIONS.md](./DEVELOPMENT_CONVENTIONS.md)                       | Naming, formatting, import order, error handling, logging conventions                                                                             |
| [COMPONENT_INVENTORY.md](./COMPONENT_INVENTORY.md)                               | Registry of every UI component — prevents duplicate components                                                                                    |
| [ROUTES.md](./ROUTES.md)                                                         | Complete routing map — public, admin, teacher, guards                                                                                             |
| [FEATURE_STATUS.md](./FEATURE_STATUS.md)                                         | Per-feature status dashboard (status, owner, dependencies, risk)                                                                                  |
| [DEFINITION_OF_DONE.md](./DEFINITION_OF_DONE.md)                                 | Full completion checklist for any unit of work                                                                                                    |
| [AI_RULES.md](./AI_RULES.md)                                                     | Behavioral rules for AI assistants working on this codebase                                                                                       |
| [MASTER_PROMPT.md](./MASTER_PROMPT.md)                                           | Universal implementation prompt every future task-specific prompt extends                                                                         |
| [DECISIONS.md](./DECISIONS.md)                                                   | Log of every approved product/architecture decision                                                                                               |
| [IMPLEMENTATION_LOG.md](./IMPLEMENTATION_LOG.md)                                 | Engineering diary — why, alternatives, lessons learned                                                                                            |
| [ROADMAP.md](./ROADMAP.md)                                                       | Phased delivery plan, Phase 0A through Phase 5                                                                                                    |
| [TASKS.md](./TASKS.md)                                                           | Live sprint task board                                                                                                                            |
| [CHANGELOG.md](./CHANGELOG.md)                                                   | Version history (Keep a Changelog format)                                                                                                         |
| [README.md](../README.md)                                                        | Project overview, quick start, documentation map                                                                                                  |

---

## 15. How Future AI Assistants Should Use This Document

1. **Read this file first**, always. It is the anchor for every other decision. Read [MASTER_PROMPT.md](./MASTER_PROMPT.md) next if the task is implementation work.
2. **Do not contradict recorded decisions.** If a request conflicts with [DECISIONS.md](./DECISIONS.md), flag the conflict instead of silently overriding it.
3. **Do not expand scope.** If asked to add a feature outside Version 1 scope, point to [PRODUCT_REQUIREMENTS.md § Out of Scope](./PRODUCT_REQUIREMENTS.md#4-out-of-scope) and [PROJECT_GUARDRAILS.md](./PROJECT_GUARDRAILS.md), and confirm with the user before proceeding.
4. **Update this document at the end of every phase.** Current Phase, Pending Features/Completed Features, Current Risks, and Important Decisions sections must stay accurate — treat inaccuracies here as bugs.
5. **Follow [AI_RULES.md](./AI_RULES.md)** for behavioral rules and [DEVELOPMENT_CONVENTIONS.md](./DEVELOPMENT_CONVENTIONS.md) for code style once implementation begins.
6. **Check [COMPONENT_INVENTORY.md](./COMPONENT_INVENTORY.md) before creating any component**, and [ROUTES.md](./ROUTES.md) before adding any route.
7. **Verify work against [DEFINITION_OF_DONE.md](./DEFINITION_OF_DONE.md)** before considering anything complete.
8. **When uncertain, ask.** This project prioritizes correctness and consistency over speed of output.
